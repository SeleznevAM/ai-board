---
phase: 01-youtrack-data-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - vitest.config.ts
  - src/features/youtrack-scope/domain/scope-types.ts
  - src/features/youtrack-scope/domain/scope-contract.ts
  - src/features/youtrack-scope/domain/scope-contract.test.ts
autonomous: true
requirements:
  - YTSC-01
  - YTSC-02
must_haves:
  truths:
    - "Root issue input is accepted as a first-class request contract, not an ad hoc string passed through the UI."
    - "Phase 1 has typed, distinct outcomes for not-found, access-denied, partial-visibility, and single-node success per D-04 through D-08."
    - "The supported scope semantics are locked to root-plus-subtasks only per D-01 and D-02."
  artifacts:
    - path: "src/features/youtrack-scope/domain/scope-types.ts"
      provides: "Typed request, node, exclusion, and resolution result contracts for Phase 1."
    - path: "src/features/youtrack-scope/domain/scope-contract.ts"
      provides: "Pure helpers that enforce the Phase 1 root issue and visibility semantics."
    - path: "src/features/youtrack-scope/domain/scope-contract.test.ts"
      provides: "Executable examples for allowed success and failure branches."
  key_links:
    - from: "src/features/youtrack-scope/domain/scope-contract.ts"
      to: "src/features/youtrack-scope/domain/scope-types.ts"
      via: "typed imports and returned discriminated unions"
      pattern: "from './scope-types'"
    - from: "src/features/youtrack-scope/domain/scope-contract.test.ts"
      to: "src/features/youtrack-scope/domain/scope-contract.ts"
      via: "domain contract assertions"
      pattern: "describe\\('scope contract'"
---

<objective>
Create the minimum runnable app/test shell and lock the Phase 1 domain contract before any YouTrack traversal code exists.

Purpose: Give downstream implementation a stable request/result model for root issue parsing, tree completeness, and failure semantics so later plans do not invent them ad hoc.
Output: Bootstrapped Next.js/Vitest workspace plus typed Phase 1 scope contract and tests.
</objective>

<execution_context>
@/Users/alexanderseleznev/.codex/get-shit-done/workflows/execute-plan.md
@/Users/alexanderseleznev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-youtrack-data-contract/01-CONTEXT.md
@.planning/phases/01-youtrack-data-contract/01-RESEARCH.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap the minimal runnable shell for Phase 1</name>
  <files>package.json, tsconfig.json, next.config.ts, vitest.config.ts</files>
  <read_first>.planning/PROJECT.md, .planning/research/STACK.md, .planning/phases/01-youtrack-data-contract/01-CONTEXT.md</read_first>
  <action>Create the narrowest possible Next.js 15 + React 19 + TypeScript 5.8 + Vitest workspace needed to implement and test Phase 1. Include scripts for `dev`, `build`, `test`, and `lint` only. Do not add Prisma, PostgreSQL, worklog ingestion, profitability math, or scenario-editing scaffolding in this phase. Keep the shell focused on a single-page flow that can host the root-issue input and tree result later.</action>
  <verify>
    <automated>pnpm install && pnpm test</automated>
  </verify>
  <acceptance_criteria>The repo installs without manual patching, `pnpm test` can execute at least one Vitest suite, and the project shell contains no Phase 2+ concerns such as worklogs, budgets, or profitability services.</acceptance_criteria>
  <done>Runnable application/test foundation exists and is narrow enough to host only the Phase 1 scope contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define the Phase 1 scope contract and failure semantics</name>
  <files>src/features/youtrack-scope/domain/scope-types.ts, src/features/youtrack-scope/domain/scope-contract.ts, src/features/youtrack-scope/domain/scope-contract.test.ts</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/phases/01-youtrack-data-contract/01-CONTEXT.md, .planning/phases/01-youtrack-data-contract/01-RESEARCH.md</read_first>
  <action>Per D-01 through D-08, create a pure domain contract that models: request input by human-readable root issue key, supported scope as root plus recursive subtasks only, result unions for `success`, `root_not_found`, `root_forbidden`, and `partial_visibility_blocked`, and a success case where the root may be the only node per D-08. Add tests that prove non-hierarchical links are outside the model per D-02 and never required to appear in exclusions per D-03. Keep this module UI-agnostic and network-agnostic so later plans can consume it directly.</action>
  <verify>
    <automated>pnpm vitest run src/features/youtrack-scope/domain/scope-contract.test.ts</automated>
  </verify>
  <acceptance_criteria>The contract exposes typed outcomes for all required root issue branches, single-node trees are valid success results, and unsupported non-hierarchical links are absent from the contract surface instead of being partially modeled.</acceptance_criteria>
  <done>Phase 1 semantics are frozen in code and protected by tests before traversal or UI wiring begins.</done>
</task>

</tasks>

<verification>
Run the domain contract tests and confirm the workspace can execute them from a clean install.
</verification>

<success_criteria>
The repository has a runnable Next.js/Vitest shell and a tested Phase 1 domain contract that downstream plans can import without redefining scope or error semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/01-youtrack-data-contract/01-youtrack-data-contract-01-SUMMARY.md`
</output>
