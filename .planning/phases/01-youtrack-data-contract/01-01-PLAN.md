---
phase: 01-youtrack-data-contract
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - vitest.config.ts
  - app/layout.tsx
  - app/page.tsx
  - src/lib/env.ts
  - src/lib/youtrack/contracts.ts
  - src/lib/youtrack/errors.ts
  - src/lib/scope/types.ts
  - src/lib/scope/policy.ts
autonomous: true
requirements:
  - YTSC-01
  - YTSC-02
user_setup: []
must_haves:
  truths:
    - "User can open the phase-one page and see the root-issue scope discovery screen"
    - "User can see that phase one supports subtasks-only scope and excludes non-hierarchical links"
    - "Phase-one root issue outcomes are represented as distinct states for not-found, forbidden, partial-scope-blocked, and single-node success cases"
  artifacts:
    - path: "package.json"
      provides: "Next.js application manifest for the project"
      contains: "\"next\""
    - path: "vitest.config.ts"
      provides: "Runnable Vitest test shell for phase-one regression coverage"
      contains: "defineConfig"
    - path: "src/lib/youtrack/contracts.ts"
      provides: "Typed YouTrack issue and hierarchy contract for phase one"
      min_lines: 40
      contains: "export type YouTrackIssueNode"
    - path: "src/lib/scope/policy.ts"
      provides: "Scope policy that enforces subtasks-only traversal and partial-visibility blocking"
      min_lines: 40
      contains: "export const SCOPE_POLICY"
  key_links:
    - from: "app/page.tsx"
      to: "src/lib/scope/policy.ts"
      via: "phase-one page copy references scope semantics"
      pattern: "subtasks-only|subtasks only"
    - from: "src/lib/scope/types.ts"
      to: "src/lib/youtrack/contracts.ts"
      via: "scope tree types wrap YouTrack issue nodes"
      pattern: "YouTrackIssueNode"
---

<objective>
Create the phase-one application and domain foundation for YouTrack scope discovery.

Purpose: Establish one consistent contract for root issue input, subtasks-only scope, and explicit root issue failure modes before any traversal or UI rendering logic is built.
Output: Bootstrapped app shell plus typed phase-one contracts, policies, and error models that later plans can implement against.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-youtrack-data-contract/01-CONTEXT.md
@.planning/phases/01-youtrack-data-contract/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap the phase-one web app shell</name>
  <files>package.json, tsconfig.json, next.config.ts, vitest.config.ts, app/layout.tsx, app/page.tsx</files>
  <read_first>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/phases/01-youtrack-data-contract/01-CONTEXT.md, .planning/phases/01-youtrack-data-contract/01-RESEARCH.md</read_first>
  <action>Create the initial Next.js TypeScript application shell for the repository. Add `package.json` with `next`, `react`, `react-dom`, `typescript`, `vitest`, and `@types/node`, plus the standard `dev`, `build`, `start`, `lint`, and `test` scripts where `test` runs `vitest run`. Add `tsconfig.json` configured for Next.js TypeScript. Add `next.config.ts` as the app-level Next config file. Add `vitest.config.ts` using `defineConfig` so later plans can run regression tests. Create `app/layout.tsx` with a minimal HTML shell and `app/page.tsx` as the phase-one entry page that contains a heading for YouTrack scope discovery, a short explanation that v1 uses subtasks-only scope, and a placeholder region where root issue input and tree results will be rendered in later plans.</action>
  <verify>test -f package.json && test -f vitest.config.ts && test -f app/page.tsx && grep -q "\"test\": \"vitest run\"" package.json && grep -q "subtasks-only" app/page.tsx && npm run test && npm run build</verify>
  <acceptance_criteria>
    - `package.json` contains `"next"` and `"dev": "next dev"`
    - `package.json` contains `"test": "vitest run"`
    - `vitest.config.ts` contains `defineConfig`
    - `app/layout.tsx` contains `export default function RootLayout`
    - `app/page.tsx` contains `YouTrack scope discovery`
    - `app/page.tsx` contains `subtasks-only`
  </acceptance_criteria>
  <done>The repository contains a runnable Next.js app shell with the phase-one landing page copy committed in source.</done>
</task>

<task type="auto">
  <name>Task 2: Define phase-one YouTrack and scope contracts</name>
  <files>src/lib/env.ts, src/lib/youtrack/contracts.ts, src/lib/youtrack/errors.ts, src/lib/scope/types.ts, src/lib/scope/policy.ts</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/phases/01-youtrack-data-contract/01-CONTEXT.md, .planning/phases/01-youtrack-data-contract/01-RESEARCH.md</read_first>
  <action>Create `src/lib/env.ts` with typed environment access for the future YouTrack base URL and auth/session assumptions. Create `src/lib/youtrack/contracts.ts` with exported types for `YouTrackIssueNode`, `RootIssueLookupResult`, and any minimal API payload shapes needed for phase one. Create `src/lib/youtrack/errors.ts` with distinct error classes or discriminated error variants for `ROOT_ISSUE_NOT_FOUND`, `ROOT_ISSUE_FORBIDDEN`, and `PARTIAL_SCOPE_FORBIDDEN`. Create `src/lib/scope/types.ts` with typed scope-tree nodes and scope status values. Create `src/lib/scope/policy.ts` that exports a `SCOPE_POLICY` constant documenting these exact rules: only parent/subtask hierarchy is supported, non-hierarchical links are ignored, partial visibility blocks successful results, and a root issue without children remains a valid one-node tree.</action>
  <verify>grep -q "export type YouTrackIssueNode" src/lib/youtrack/contracts.ts && grep -q "PARTIAL_SCOPE_FORBIDDEN" src/lib/youtrack/errors.ts && grep -q "export const SCOPE_POLICY" src/lib/scope/policy.ts</verify>
  <acceptance_criteria>
    - `src/lib/youtrack/contracts.ts` contains `export type YouTrackIssueNode`
    - `src/lib/youtrack/errors.ts` contains `ROOT_ISSUE_NOT_FOUND`
    - `src/lib/youtrack/errors.ts` contains `ROOT_ISSUE_FORBIDDEN`
    - `src/lib/youtrack/errors.ts` contains `PARTIAL_SCOPE_FORBIDDEN`
    - `src/lib/scope/policy.ts` contains `non-hierarchical links are ignored`
    - `src/lib/scope/policy.ts` contains `root issue without children remains valid`
  </acceptance_criteria>
  <done>The codebase has one source of truth for phase-one scope semantics, typed issue data, and explicit root issue error cases.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `test -f package.json && test -f app/page.tsx`
- [ ] `grep -q "\"test\": \"vitest run\"" package.json`
- [ ] `npm run test`
- [ ] `npm run build`
- [ ] `grep -q "export const SCOPE_POLICY" src/lib/scope/policy.ts`
- [ ] `grep -q "PARTIAL_SCOPE_FORBIDDEN" src/lib/youtrack/errors.ts`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase-one app shell includes a runnable Vitest test command
- Phase-one domain code contains one explicit subtasks-only scope policy
- Root issue error cases are represented as separate typed states
  </success_criteria>

<output>
After completion, create `.planning/phases/01-youtrack-data-contract/01-01-SUMMARY.md`
</output>
