---
phase: 01-youtrack-data-contract
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/features/youtrack-scope/server/youtrack-scope-source.ts
  - src/features/youtrack-scope/server/resolve-scope-tree.ts
  - src/features/youtrack-scope/server/resolve-scope-tree.test.ts
  - src/features/youtrack-scope/server/testing/fake-youtrack-scope-source.ts
autonomous: true
requirements:
  - YTSC-02
  - YTSC-03
must_haves:
  truths:
    - "Scope resolution walks the supported hierarchy recursively and stops treating the result as valid when any supported descendant is inaccessible."
    - "Traversal is defensive against duplicate visits or cycles so future calculations cannot double-count nodes."
    - "Successful scope results contain only supported hierarchy nodes and never include arbitrary linked issues per D-02."
  artifacts:
    - path: "src/features/youtrack-scope/server/youtrack-scope-source.ts"
      provides: "Server-side boundary for fetching a root issue and its visible subtasks."
    - path: "src/features/youtrack-scope/server/resolve-scope-tree.ts"
      provides: "Recursive scope resolver with completeness checks and defensive traversal."
    - path: "src/features/youtrack-scope/server/resolve-scope-tree.test.ts"
      provides: "Tests for full tree, missing root, forbidden root, partial visibility, duplicate, and cycle scenarios."
  key_links:
    - from: "src/features/youtrack-scope/server/resolve-scope-tree.ts"
      to: "src/features/youtrack-scope/domain/scope-contract.ts"
      via: "result construction through Phase 1 contract helpers"
      pattern: "scope-contract"
    - from: "src/features/youtrack-scope/server/resolve-scope-tree.test.ts"
      to: "src/features/youtrack-scope/server/testing/fake-youtrack-scope-source.ts"
      via: "deterministic traversal fixtures"
      pattern: "fake-youtrack-scope-source"
---

<objective>
Implement the server-side scope resolver that applies the Phase 1 contract to a recursive YouTrack-like source without expanding beyond the allowed hierarchy semantics.

Purpose: Turn the frozen domain semantics into executable traversal behavior that future UI and API layers can trust.
Output: YouTrack scope source boundary, defensive resolver, and tests covering full and failed tree resolution.
</objective>

<execution_context>
@/Users/alexanderseleznev/.codex/get-shit-done/workflows/execute-plan.md
@/Users/alexanderseleznev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-youtrack-data-contract/01-CONTEXT.md
@.planning/phases/01-youtrack-data-contract/01-RESEARCH.md
@.planning/phases/01-youtrack-data-contract/01-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the minimal YouTrack scope source boundary</name>
  <files>src/features/youtrack-scope/server/youtrack-scope-source.ts, src/features/youtrack-scope/server/testing/fake-youtrack-scope-source.ts</files>
  <read_first>.planning/phases/01-youtrack-data-contract/01-01-PLAN.md, .planning/phases/01-youtrack-data-contract/01-CONTEXT.md, .planning/phases/01-youtrack-data-contract/01-RESEARCH.md</read_first>
  <action>Create a narrow server-side interface that resolves a root issue by readable key and returns only the data needed for Phase 1: node identity, summary text, direct subtask references, and visibility/completeness signals. Per D-04 and D-05, the source contract must preserve current-user access behavior instead of masking it behind a privileged abstraction. The fake implementation must support tests for not-found, forbidden, partial-visibility, duplicate-child, and cycle-defense scenarios.</action>
  <verify>
    <automated>pnpm vitest run src/features/youtrack-scope/server/resolve-scope-tree.test.ts --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>The source boundary is small enough that it cannot accidentally expose worklogs or unsupported links, and test fixtures can represent all Phase 1 access/visibility branches deterministically.</acceptance_criteria>
  <done>Traversal code has a single, explicit input boundary for Phase 1 scope data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement defensive recursive scope resolution</name>
  <files>src/features/youtrack-scope/server/resolve-scope-tree.ts, src/features/youtrack-scope/server/resolve-scope-tree.test.ts</files>
  <read_first>.planning/phases/01-youtrack-data-contract/01-01-PLAN.md, src/features/youtrack-scope/domain/scope-contract.ts, src/features/youtrack-scope/server/youtrack-scope-source.ts</read_first>
  <action>Implement a resolver that starts from the human-entered root issue key, expands only through subtasks per D-01 and D-02, and produces the domain result unions from Plan 01. Per D-05, if any supported descendant is inaccessible or the source reports incomplete visibility, return `partial_visibility_blocked` instead of a partial success tree. Add cycle and duplicate protection so a repeated node key is visited once and recorded as a defensive resolution event rather than duplicated in the final tree. Do not ingest worklogs, costs, or any non-tree diagnostics in this resolver.</action>
  <verify>
    <automated>pnpm vitest run src/features/youtrack-scope/server/resolve-scope-tree.test.ts</automated>
  </verify>
  <acceptance_criteria>Tests prove recursive success across nested subtasks, single-node success when the root has no children, distinct not-found and forbidden failures, hard failure on partial visibility, and no duplicated nodes when the source contains repeated references.</acceptance_criteria>
  <done>Phase 1 has an executable and tested scope resolver that enforces the supported traversal and completeness rules.</done>
</task>

</tasks>

<verification>
Run the resolver test suite and confirm the partial-visibility branch blocks a success payload.
</verification>

<success_criteria>
The server layer can resolve a root issue into a supported tree or a typed Phase 1 failure without leaking unsupported links or double-counting nodes.
</success_criteria>

<output>
After completion, create `.planning/phases/01-youtrack-data-contract/01-youtrack-data-contract-02-SUMMARY.md`
</output>
