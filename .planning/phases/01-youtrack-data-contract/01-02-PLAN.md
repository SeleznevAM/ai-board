---
phase: 01-youtrack-data-contract
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/lib/youtrack/client.ts
  - src/lib/youtrack/auth.ts
  - src/lib/scope/buildScopeTree.ts
  - src/lib/scope/buildScopeTree.test.ts
  - app/api/scope/route.ts
autonomous: true
requirements:
  - YTSC-02
  - YTSC-03
user_setup: []
must_haves:
  truths:
    - "The system resolves scope only through the root issue and its nested subtasks"
    - "A root issue with inaccessible descendants is treated as a blocked scope, not a partial success"
    - "Duplicate or cyclic issue references do not duplicate tree nodes or create infinite traversal"
  artifacts:
    - path: "src/lib/youtrack/client.ts"
      provides: "YouTrack API client abstraction for root issue and subtask discovery"
      min_lines: 50
      contains: "export async function fetchRootIssueTree"
    - path: "src/lib/youtrack/auth.ts"
      provides: "Current-user YouTrack access resolution for phase-one requests"
      min_lines: 30
      contains: "export async function resolveCurrentUserYouTrackAccess"
    - path: "src/lib/scope/buildScopeTree.ts"
      provides: "Scope-tree builder enforcing phase-one visibility and hierarchy rules"
      min_lines: 70
      contains: "export async function buildScopeTree"
    - path: "app/api/scope/route.ts"
      provides: "HTTP endpoint that returns either a valid scope tree or a phase-one error"
      exports:
        - "POST"
  key_links:
    - from: "app/api/scope/route.ts"
      to: "src/lib/scope/buildScopeTree.ts"
      via: "route invokes scope builder"
      pattern: "buildScopeTree"
    - from: "app/api/scope/route.ts"
      to: "src/lib/youtrack/auth.ts"
      via: "route resolves current-user YouTrack access before calling the client"
      pattern: "resolveCurrentUserYouTrackAccess"
    - from: "src/lib/scope/buildScopeTree.ts"
      to: "src/lib/youtrack/client.ts"
      via: "scope builder fetches root and child issues"
      pattern: "fetchRootIssueTree|fetchIssue"
---

<objective>
Implement the technical scope-resolution path for phase one.

Purpose: Turn the static phase-one contract into executable server logic that can resolve a root issue, walk subtasks, detect blocked visibility, and return a deterministic scope tree result.
Output: YouTrack client abstraction, scope-tree builder, route handler, and tests for the required root issue and visibility edge cases.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-youtrack-data-contract/01-CONTEXT.md
@.planning/phases/01-youtrack-data-contract/01-RESEARCH.md
@src/lib/youtrack/contracts.ts
@src/lib/youtrack/errors.ts
@src/lib/scope/types.ts
@src/lib/scope/policy.ts
@vitest.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement root issue and subtask scope resolution</name>
  <files>src/lib/youtrack/client.ts, src/lib/youtrack/auth.ts, src/lib/scope/buildScopeTree.ts</files>
  <read_first>src/lib/youtrack/contracts.ts, src/lib/youtrack/errors.ts, src/lib/scope/types.ts, src/lib/scope/policy.ts, .planning/phases/01-youtrack-data-contract/01-CONTEXT.md, .planning/phases/01-youtrack-data-contract/01-RESEARCH.md</read_first>
  <action>Create `src/lib/youtrack/auth.ts` with `resolveCurrentUserYouTrackAccess(request)` that extracts the current user's YouTrack access context from the incoming request/session and returns the credentials or token material that may be used for phase-one calls. Create `src/lib/youtrack/client.ts` with exported functions that accept this current-user access context, resolve a user-supplied root issue key into the minimal issue payload, and fetch child subtasks needed for recursive tree construction. Create `src/lib/scope/buildScopeTree.ts` with `buildScopeTree` that uses only parent/subtask hierarchy, tracks visited issue ids to avoid duplicates or cycles, returns a one-node tree when the root has no children, throws the explicit not-found or forbidden root issue errors from `src/lib/youtrack/errors.ts`, and raises `PARTIAL_SCOPE_FORBIDDEN` whenever any required descendant in the supported hierarchy cannot be read by the current user.</action>
  <verify>grep -q "export async function resolveCurrentUserYouTrackAccess" src/lib/youtrack/auth.ts && grep -q "export async function buildScopeTree" src/lib/scope/buildScopeTree.ts && grep -Eq "visited|seen" src/lib/scope/buildScopeTree.ts && grep -q "PARTIAL_SCOPE_FORBIDDEN" src/lib/scope/buildScopeTree.ts</verify>
  <acceptance_criteria>
    - `src/lib/youtrack/auth.ts` contains `export async function resolveCurrentUserYouTrackAccess`
    - `src/lib/youtrack/client.ts` contains `export async function fetchRootIssueTree`
    - `src/lib/youtrack/client.ts` contains `currentUserAccess`
    - `src/lib/scope/buildScopeTree.ts` contains `export async function buildScopeTree`
    - `src/lib/scope/buildScopeTree.ts` contains `PARTIAL_SCOPE_FORBIDDEN`
    - `src/lib/scope/buildScopeTree.ts` contains `ROOT_ISSUE_NOT_FOUND`
    - `src/lib/scope/buildScopeTree.ts` contains `ROOT_ISSUE_FORBIDDEN`
    - `src/lib/scope/buildScopeTree.ts` contains `visited`
  </acceptance_criteria>
  <done>The server-side scope builder enforces every phase-one root issue and visibility rule from the context and research files.</done>
</task>

<task type="auto">
  <name>Task 2: Add API contract and regression tests for phase-one edge cases</name>
  <files>app/api/scope/route.ts, src/lib/scope/buildScopeTree.test.ts</files>
  <read_first>src/lib/scope/buildScopeTree.ts, src/lib/youtrack/auth.ts, src/lib/youtrack/contracts.ts, src/lib/youtrack/errors.ts, .planning/REQUIREMENTS.md</read_first>
  <action>Create `app/api/scope/route.ts` with a `POST` handler that accepts a root issue identifier, calls `resolveCurrentUserYouTrackAccess(request)` before any YouTrack lookup, passes the resolved current-user access into `buildScopeTree`, returns a successful JSON payload only for a complete visible tree, maps `ROOT_ISSUE_NOT_FOUND` to a not-found response, maps `ROOT_ISSUE_FORBIDDEN` to an access-denied response, and maps `PARTIAL_SCOPE_FORBIDDEN` to a blocked scope response that does not include a partial tree payload. Create `src/lib/scope/buildScopeTree.test.ts` with regression tests that cover these exact scenarios: root issue exists and has nested subtasks, root issue exists and has no children, root issue is missing, root issue is inaccessible, descendant visibility is partial and blocks success, and duplicate or cyclic references do not produce repeated nodes. Use the Vitest shell from `package.json` and `vitest.config.ts`, and make the plan verification run `npm run test -- buildScopeTree` instead of grepping a test file.</action>
  <verify>grep -q "export async function POST" app/api/scope/route.ts && grep -q "resolveCurrentUserYouTrackAccess" app/api/scope/route.ts && grep -q "PARTIAL_SCOPE_FORBIDDEN" app/api/scope/route.ts && npm run test -- buildScopeTree</verify>
  <acceptance_criteria>
    - `app/api/scope/route.ts` contains `export async function POST`
    - `app/api/scope/route.ts` contains `resolveCurrentUserYouTrackAccess`
    - `app/api/scope/route.ts` contains `PARTIAL_SCOPE_FORBIDDEN`
    - `app/api/scope/route.ts` contains `ROOT_ISSUE_NOT_FOUND`
    - `app/api/scope/route.ts` contains `ROOT_ISSUE_FORBIDDEN`
    - `src/lib/scope/buildScopeTree.test.ts` contains `root issue exists and has no children`
    - `src/lib/scope/buildScopeTree.test.ts` contains `partial visibility`
    - `src/lib/scope/buildScopeTree.test.ts` contains `duplicate or cyclic`
    - `package.json` contains `"test": "vitest run"`
  </acceptance_criteria>
  <done>The codebase includes a stable route-level contract and explicit tests for every user-approved phase-one edge case.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `grep -q "export async function POST" app/api/scope/route.ts`
- [ ] `grep -q "resolveCurrentUserYouTrackAccess" app/api/scope/route.ts`
- [ ] `grep -q "PARTIAL_SCOPE_FORBIDDEN" src/lib/scope/buildScopeTree.ts`
- [ ] `npm run test -- buildScopeTree`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Server logic blocks partial scope instead of returning a partial success
- Route responses distinguish missing root issue, forbidden root issue, and blocked descendant visibility
  </success_criteria>

<output>
After completion, create `.planning/phases/01-youtrack-data-contract/01-02-SUMMARY.md`
</output>
