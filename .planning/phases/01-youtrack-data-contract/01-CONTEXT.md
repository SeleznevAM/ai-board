# Phase 1: YouTrack Data Contract - Context

**Gathered:** 2026-04-07
**Status:** Ready for planning

<domain>
## Phase Boundary

Фаза определяет контракт входных данных YouTrack для будущего расчета рентабельности. На выходе пользователь должен иметь возможность ввести `root issue`, увидеть дерево задач, которое система считает частью требования, и понять базовые правила, по которым scope строится или не строится.

</domain>

<decisions>
## Implementation Decisions

### Scope semantics
- **D-01:** В v1 частью требования считается только основная иерархия `родитель -> подзадачи -> их подзадачи` до самого нижнего уровня.
- **D-02:** Неиерархические связи и связанные задачи вне основной иерархии в v1 полностью игнорируются.
- **D-03:** Игнорируемые неиерархические связи не нужно явно показывать пользователю как исключенные из расчета.

### Access model
- **D-04:** Система должна работать в контексте прав текущего пользователя, который открыл приложение, а не через единый сервисный аккаунт.
- **D-05:** Если пользователю недоступна хотя бы часть дерева задач, расчет не должен показываться вообще.

### Root issue handling
- **D-06:** Если `root issue` не существует, система показывает отдельную ошибку.
- **D-07:** Если `root issue` существует, но недоступна текущему пользователю, система показывает отдельную ошибку доступа.
- **D-08:** Если у `root issue` нет дочерних элементов, система все равно строит дерево из одной корневой задачи.

### Phase output
- **D-09:** На выходе этой фазы пользователь должен видеть дерево задач с вложенностью.

### the agent's Discretion
- Точная форма визуализации дерева задач.
- Формулировки ошибок и пустых состояний при соблюдении зафиксированной логики.
- Внутренний способ обхода дерева и нормализации данных YouTrack, если он не меняет пользовательские решения выше.

</decisions>

<specifics>
## Specific Ideas

- Пользователь хочет, чтобы ранняя фаза уже давала понятный результат в виде дерева задач, а не абстрактной проверки доступности.
- Ограничение v1 принято осознанно: связанные задачи вне иерархии могут существовать в YouTrack, но в расчет первой версии не входят.
- Для доверия к цифрам выбран жесткий режим: если видимость дерева неполная, расчет не показывается.

</specifics>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Project scope
- `.planning/PROJECT.md` — общий продуктовый контекст, целевая ценность и основные ограничения v1.
- `.planning/REQUIREMENTS.md` — требования `YTSC-01`, `YTSC-02`, `YTSC-03` и связанные ограничения интерфейса и достоверности.
- `.planning/ROADMAP.md` — фазовая граница и success criteria для Phase 1.
- `.planning/STATE.md` — текущее состояние проекта и зафиксированные блокеры по Phase 1.

### Research
- `.planning/research/SUMMARY.md` — общее исследовательское резюме и риски, важные для первой фазы.
- `.planning/research/ARCHITECTURE.md` — рекомендуемые архитектурные границы вокруг YouTrack ingestion.
- `.planning/research/PITFALLS.md` — риски по семантике иерархии, правам доступа и неполному scope.

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- Готовых прикладных компонентов или кода продукта пока нет: репозиторий находится на стадии planning-only.

### Established Patterns
- Текущий проект начинается с нуля, поэтому Phase 1 может свободно задать правильный контракт данных YouTrack до появления кода и UI-паттернов.
- Интеграционно-ориентированный порядок уже зафиксирован в roadmap: сначала контракт данных, потом ingestion correctness.

### Integration Points
- Phase 1 должна подготовить основу для следующих фаз: правила обхода дерева для Phase 2 и явные условия допустимости расчета для будущего UI и cost engine.

</code_context>

<deferred>
## Deferred Ideas

- Поддержка неиерархических связей как части scope — возможное расширение будущих фаз, но вне границ v1 Phase 1.
- Отдельная индикация исключенных связанных задач вне основной иерархии — отложено, так как пользователь решил не перегружать интерфейс этой версии.
- Общий сервисный доступ вместо пользовательского контекста — сознательно не выбран для v1.

</deferred>

---
*Phase: 01-youtrack-data-contract*
*Context gathered: 2026-04-07*
