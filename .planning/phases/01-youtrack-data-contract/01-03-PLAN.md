---
phase: 01-youtrack-data-contract
plan: "03"
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - src/components/root-issue-form.tsx
  - src/components/scope-tree.tsx
  - src/components/scope-state.tsx
  - app/page.tsx
autonomous: true
requirements:
  - YTSC-01
  - YTSC-03
user_setup: []
must_haves:
  truths:
    - "A user can submit a root issue identifier from the phase-one page"
    - "A complete visible scope renders as a nested task tree"
    - "Missing, forbidden, and blocked-scope outcomes render as distinct user-facing states instead of a fake tree"
  artifacts:
    - path: "src/components/root-issue-form.tsx"
      provides: "Root issue entry form for phase-one scope discovery"
      min_lines: 30
      contains: "root issue"
    - path: "src/components/scope-tree.tsx"
      provides: "Nested tree renderer for complete scope results"
      min_lines: 40
      contains: "children"
    - path: "src/components/scope-state.tsx"
      provides: "Distinct error and blocked-result presentation for phase one"
      min_lines: 30
      contains: "PARTIAL_SCOPE_FORBIDDEN"
  key_links:
    - from: "app/page.tsx"
      to: "src/components/root-issue-form.tsx"
      via: "page renders root issue form"
      pattern: "RootIssueForm"
    - from: "app/page.tsx"
      to: "src/components/scope-tree.tsx"
      via: "page renders nested scope tree after successful lookup"
      pattern: "ScopeTree"
    - from: "app/page.tsx"
      to: "src/components/scope-state.tsx"
      via: "page renders explicit blocked and error states"
      pattern: "ScopeState"
    - from: "src/components/root-issue-form.tsx"
      to: "/api/scope"
      via: "form submits root issue identifier with POST/fetch"
      pattern: "/api/scope"
---

<objective>
Deliver the minimal user-visible experience for phase one.

Purpose: Let a user enter a root issue and see either a nested scope tree or the exact phase-one error state that prevents scope confirmation.
Output: Form, tree renderer, state renderer, and page integration wired to the phase-one API contract.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-youtrack-data-contract/01-CONTEXT.md
@.planning/phases/01-youtrack-data-contract/01-RESEARCH.md
@app/page.tsx
@app/api/scope/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the root issue entry flow and nested tree renderer</name>
  <files>src/components/root-issue-form.tsx, src/components/scope-tree.tsx, app/page.tsx</files>
  <read_first>app/page.tsx, app/api/scope/route.ts, .planning/phases/01-youtrack-data-contract/01-CONTEXT.md, .planning/phases/01-youtrack-data-contract/01-RESEARCH.md</read_first>
  <action>Create `src/components/root-issue-form.tsx` with a form that captures a single root issue identifier and posts it to `/api/scope`. Create `src/components/scope-tree.tsx` that renders the successful scope result as a nested tree using the scope node structure from Phase 1 contracts. Update `app/page.tsx` so it renders the root issue form at the top of the page, shows a short note that only the subtask hierarchy is supported in this phase, and renders `ScopeTree` only when the API response represents a complete successful scope.</action>
  <verify>grep -q "RootIssueForm" app/page.tsx && grep -q "/api/scope" src/components/root-issue-form.tsx && grep -q "children" src/components/scope-tree.tsx</verify>
  <acceptance_criteria>
    - `src/components/root-issue-form.tsx` contains `/api/scope`
    - `src/components/root-issue-form.tsx` contains `root issue`
    - `src/components/scope-tree.tsx` contains `children`
    - `app/page.tsx` contains `RootIssueForm`
    - `app/page.tsx` contains `ScopeTree`
    - `app/page.tsx` contains `only the subtask hierarchy is supported`
  </acceptance_criteria>
  <done>The phase-one page can submit a root issue and render a nested tree only for successful complete scope results.</done>
</task>

<task type="auto">
  <name>Task 2: Render distinct phase-one error and blocked states</name>
  <files>src/components/scope-state.tsx, app/page.tsx</files>
  <read_first>app/page.tsx, app/api/scope/route.ts, src/lib/youtrack/errors.ts, .planning/phases/01-youtrack-data-contract/01-CONTEXT.md</read_first>
  <action>Create `src/components/scope-state.tsx` that maps the phase-one route outcomes to distinct user-facing states for `ROOT_ISSUE_NOT_FOUND`, `ROOT_ISSUE_FORBIDDEN`, and `PARTIAL_SCOPE_FORBIDDEN`. Update `app/page.tsx` so missing root issue and forbidden root issue render separate messages, while `PARTIAL_SCOPE_FORBIDDEN` renders a blocked-result state and does not render `ScopeTree`. Keep unsupported non-hierarchical links completely absent from the UI, matching the context decision that they are ignored and not shown explicitly.</action>
  <verify>grep -q "ROOT_ISSUE_NOT_FOUND" src/components/scope-state.tsx && grep -q "ROOT_ISSUE_FORBIDDEN" src/components/scope-state.tsx && grep -q "PARTIAL_SCOPE_FORBIDDEN" src/components/scope-state.tsx</verify>
  <acceptance_criteria>
    - `src/components/scope-state.tsx` contains `ROOT_ISSUE_NOT_FOUND`
    - `src/components/scope-state.tsx` contains `ROOT_ISSUE_FORBIDDEN`
    - `src/components/scope-state.tsx` contains `PARTIAL_SCOPE_FORBIDDEN`
    - `app/page.tsx` contains `ScopeState`
    - `app/page.tsx` does not contain `non-hierarchical`
  </acceptance_criteria>
  <done>The user sees clear phase-one result states and never sees a fake successful tree when scope is blocked or the root issue is invalid.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `grep -q "RootIssueForm" app/page.tsx`
- [ ] `grep -q "ScopeState" app/page.tsx`
- [ ] `grep -q "PARTIAL_SCOPE_FORBIDDEN" src/components/scope-state.tsx`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The page renders a nested tree for complete scope results
- The page renders distinct blocked and error states without exposing ignored link types
  </success_criteria>

<output>
After completion, create `.planning/phases/01-youtrack-data-contract/01-03-SUMMARY.md`
</output>
