---
phase: 01-youtrack-data-contract
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/api/youtrack-scope/route.ts
  - src/features/youtrack-scope/ui/root-issue-form.tsx
  - src/features/youtrack-scope/ui/scope-tree.tsx
  - src/features/youtrack-scope/ui/scope-status.tsx
  - src/features/youtrack-scope/ui/scope-page.test.tsx
autonomous: true
requirements:
  - YTSC-01
  - YTSC-03
must_haves:
  truths:
    - "User can enter one root issue key and trigger Phase 1 scope resolution."
    - "User sees a nested tree when resolution succeeds, including the valid one-node root case per D-08."
    - "User sees explicit not-found, access-denied, or partial-visibility states, and no successful tree is shown when visibility is incomplete per D-05."
  artifacts:
    - path: "src/app/api/youtrack-scope/route.ts"
      provides: "HTTP boundary from the page to the scope resolver."
    - path: "src/app/page.tsx"
      provides: "Minimal Phase 1 screen hosting root issue entry and result area."
    - path: "src/features/youtrack-scope/ui/scope-tree.tsx"
      provides: "Recursive nested tree presentation for included issues only."
  key_links:
    - from: "src/app/api/youtrack-scope/route.ts"
      to: "src/features/youtrack-scope/server/resolve-scope-tree.ts"
      via: "route handler invocation"
      pattern: "resolveScopeTree"
    - from: "src/app/page.tsx"
      to: "/api/youtrack-scope"
      via: "single root-issue submit flow"
      pattern: "/api/youtrack-scope"
    - from: "src/features/youtrack-scope/ui/scope-tree.tsx"
      to: "src/features/youtrack-scope/domain/scope-types.ts"
      via: "typed recursive tree props"
      pattern: "ScopeNode"
---

<objective>
Expose the Phase 1 contract to the user through a single-page root issue flow and a minimal nested tree presentation.

Purpose: Deliver the first user-visible result of the product without widening scope beyond the supported tree semantics and explicit blocking failures.
Output: API route, root issue form, error/status presentation, and recursive tree UI with tests.
</objective>

<execution_context>
@/Users/alexanderseleznev/.codex/get-shit-done/workflows/execute-plan.md
@/Users/alexanderseleznev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-youtrack-data-contract/01-CONTEXT.md
@.planning/phases/01-youtrack-data-contract/01-RESEARCH.md
@.planning/phases/01-youtrack-data-contract/01-01-PLAN.md
@.planning/phases/01-youtrack-data-contract/01-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the root issue request flow from page to resolver</name>
  <files>src/app/api/youtrack-scope/route.ts, src/app/page.tsx, src/features/youtrack-scope/ui/root-issue-form.tsx, src/features/youtrack-scope/ui/scope-status.tsx</files>
  <read_first>.planning/phases/01-youtrack-data-contract/01-01-PLAN.md, .planning/phases/01-youtrack-data-contract/01-02-PLAN.md, .planning/phases/01-youtrack-data-contract/01-CONTEXT.md</read_first>
  <action>Create one Phase 1 page with a single root-issue input and submit action for YTSC-01. Add a route handler that validates the root issue key using the Plan 01 contract and calls the Plan 02 resolver. Map result unions to explicit user-facing states per D-06 and D-07, plus a hard-blocking partial-visibility status per D-05. Keep the screen intentionally narrow: no dashboards, no worklog summaries, no budget fields, and no unsupported-link diagnostics beyond the blocking status.</action>
  <verify>
    <automated>pnpm vitest run src/features/youtrack-scope/ui/scope-page.test.tsx</automated>
  </verify>
  <acceptance_criteria>The user can submit exactly one root issue key, the route returns typed Phase 1 responses, and the UI shows mutually exclusive status branches for success, not found, access denied, and partial visibility.</acceptance_criteria>
  <done>The product has a working Phase 1 entry flow that reaches the scope resolver through a stable HTTP boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Render the minimal nested tree presentation</name>
  <files>src/features/youtrack-scope/ui/scope-tree.tsx, src/features/youtrack-scope/ui/scope-page.test.tsx, src/app/layout.tsx</files>
  <read_first>.planning/phases/01-youtrack-data-contract/01-CONTEXT.md, src/features/youtrack-scope/domain/scope-types.ts, src/features/youtrack-scope/server/resolve-scope-tree.ts</read_first>
  <action>Per D-09, render successful scope results as a nested tree with indentation that makes parent-child depth obvious. The tree must show included hierarchy nodes only and remain valid when the root is the only node per D-08. Do not surface ignored non-hierarchical links as exclusions per D-03. Add UI tests proving that success renders nested nodes, single-node roots still render, and blocking failures suppress the tree entirely.</action>
  <verify>
    <automated>pnpm vitest run src/features/youtrack-scope/ui/scope-page.test.tsx</automated>
  </verify>
  <acceptance_criteria>Successful responses render a readable nested tree, one-node requirements still show a root tree entry, and failure branches never leave a stale or partial tree visible on screen.</acceptance_criteria>
  <done>The first user-visible version of the product accurately presents the supported YouTrack scope and its blocking failures.</done>
</task>

</tasks>

<verification>
Run the UI test suite and confirm that successful trees, root-only trees, and blocking errors behave as mutually exclusive screen states.
</verification>

<success_criteria>
A user can enter a root issue, receive either a nested supported tree or an explicit blocking error, and never see unsupported or partial scope presented as valid.
</success_criteria>

<output>
After completion, create `.planning/phases/01-youtrack-data-contract/01-youtrack-data-contract-03-SUMMARY.md`
</output>
