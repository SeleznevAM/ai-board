---
phase: 05-pm-dashboard
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/dashboard-status.ts
  - src/components/pm-dashboard-tabs.tsx
  - src/components/requirement-decision-card.tsx
autonomous: true
requirements: [UI-01, UI-03]
must_haves:
  truths:
    - "The dashboard exposes a desktop-first two-tab shell with `Обзор` first and `Направления` second before page wiring begins."
    - "The top requirement decision card shows exactly three primary blocks: budget, labor, and margin, with scenario values rendered as secondary values inside those blocks instead of separate forecast cards."
    - "Profitability health is expressed through whole-block status styling derived from the 20% threshold, never by coloring only the percentage text."
  artifacts:
    - path: "src/components/dashboard-status.ts"
      provides: "Reusable presentation-only helper for 20% threshold status semantics."
    - path: "src/components/pm-dashboard-tabs.tsx"
      provides: "Accessible two-tab shell for `Обзор` and `Направления` without owning business data."
    - path: "src/components/requirement-decision-card.tsx"
      provides: "Compact desktop decision card for budget, labor, and margin with inline scenario secondary values."
  key_links:
    - from: "src/components/requirement-decision-card.tsx"
      to: "src/lib/scenario/types.ts"
      via: "Consumes current-vs-scenario comparisons without recomputing forecast math"
      pattern: "ScenarioComparison|RequirementProfitability"
    - from: "src/components/dashboard-status.ts"
      to: "src/lib/costing/types.ts"
      via: "Maps profitability percentage to neutral/good/risk presentation states using the locked 20% rule"
      pattern: "marginPercent|20"
    - from: "src/components/pm-dashboard-tabs.tsx"
      to: "app/page.tsx"
      via: "Controlled tab selection keeps page-level source-of-truth state in the page instead of the shell"
      pattern: "selectedTab|onTabChange"
---

<objective>
Create the reusable Phase 05 dashboard presentation contract before touching the page composition.

Purpose: keep Phase 5 as a UI refactor over the existing Phase 4 state and calculation model, not a new domain layer.
Output: a desktop-first tab shell, reusable 20%-threshold status styling, and a compact top decision card ready for page wiring.
</objective>

<execution_context>
@/Users/alexanderseleznev/.codex/get-shit-done/workflows/execute-plan.md
@/Users/alexanderseleznev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-pm-dashboard/05-CONTEXT.md
@.planning/phases/05-pm-dashboard/05-RESEARCH.md
@.planning/phases/04-scenario-editing/04-03-SUMMARY.md
@app/page.tsx
@src/components/cost-summary.tsx
@src/components/direction-breakdown.tsx
@src/lib/costing/types.ts
@src/lib/scenario/types.ts

<interfaces>
From src/lib/costing/types.ts:
```ts
export type RequirementProfitability = {
  readonly budget: number | null;
  readonly cost: number | null;
  readonly delta: number | null;
  readonly marginPercent: number | null;
  readonly neededUpsell: number | null;
};
```

From src/lib/scenario/types.ts:
```ts
export type ScenarioComparison<TValue> = {
  readonly current: TValue;
  readonly forecast: TValue;
};
```

From app/page.tsx:
```ts
const scenarioDirty = scenarioState ? hasScenarioChanges(scenarioState) : false;
const scenarioForecast = useMemo(() => {
  // derives forecast from existing page-owned state and helpers
}, [...]);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create presentation-only dashboard status and tab-shell primitives</name>
  <files>src/components/dashboard-status.ts, src/components/pm-dashboard-tabs.tsx</files>
  <action>Create a small status helper and a controlled tabs component that implement the locked layout decisions without taking ownership of page data. Per D-01, D-02, D-11, D-13, and D-14, the tab shell must expose exactly two tabs, `Обзор` first and `Направления` second, with ARIA-compliant roles, desktop-first spacing, and a controlled API (`selectedTab`, `onTabChange`, slotted tab panels) so `app/page.tsx` stays the source of truth. In `dashboard-status.ts`, centralize the presentation mapping for the 20% threshold into neutral/good/risk styles that tint an entire block or row. Do not add a UI library, do not fetch data, and do not move any costing or scenario logic out of `app/page.tsx`.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>The repo has reusable status and tabs primitives that compile cleanly and are ready to receive existing page-owned data without changing the Phase 4 calculation boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Build the compact requirement decision card for Overview</name>
  <files>src/components/requirement-decision-card.tsx, src/components/dashboard-status.ts</files>
  <action>Create a new compact desktop decision card that replaces the current pair-of-cards summary pattern with three primary blocks: `бюджет`, `трудозатраты`, and `рентабельность`, per D-04, D-05, and D-06. The component must consume already-derived current and forecast values only; it must not recalculate any money or percentages. Per D-07, D-08, and D-09, show secondary scenario values inline inside the relevant blocks only when they add information: labor and margin secondary values appear when scenario extra hours affect forecast, and budget may show a secondary scenario value only when the scenario budget differs from baseline. Use whole-block status styling from the helper so margin health is readable at a glance and not reduced to colored text. Do not revive the old separate `Current`/`Forecast` summary cards and do not couple the component to root issue lookup, scope-tree editing, or provenance layout.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>A reusable top decision card exists for Phase 05 and can render current plus scenario-aware secondary values inside three dense blocks without altering the existing data model.</done>
</task>

</tasks>

<verification>
Run `npm run build` after each task. The plan is complete when the new dashboard primitives compile and nothing in the cost/scenario domain layer changes.
</verification>

<success_criteria>
Phase 05 has reusable Overview-first dashboard primitives that encode the locked tab order, the three-block decision card structure, and full-block threshold semantics while keeping all real state and calculation ownership in the existing page.
</success_criteria>

<output>
After completion, create `.planning/phases/05-pm-dashboard/05-01-SUMMARY.md`
</output>
