---
phase: 05-pm-dashboard
plan: "02"
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - app/page.tsx
  - src/components/direction-breakdown.tsx
  - src/components/scenario-origin-panel.tsx
autonomous: false
requirements: [UI-01, UI-02, UI-03, UI-04]
must_haves:
  truths:
    - "The first thing a PM sees on desktop is the compact `Обзор` tab with the top decision card, not the long Phase 4 page flow."
    - "The compact top decision card still exposes the required missing amount to reach 20% alongside budget, labor, and margin."
    - "Detailed direction diagnostics live in the second `Направления` tab as a dense scan surface with full-row status semantics and contribution cues."
    - "The page continues to own canonical snapshot state, scenario overlay state, and all forecast/costing calculations; Phase 5 only redistributes presentation."
  artifacts:
    - path: "app/page.tsx"
      provides: "Phase 05 page composition that reuses existing page-owned snapshot and scenario state under the new dashboard shell."
    - path: "src/components/direction-breakdown.tsx"
      provides: "Dense desktop directions view with budget, actual hours, extra hours, cost, profitability, and row-wide status semantics."
    - path: "src/components/scenario-origin-panel.tsx"
      provides: "Compacted provenance surface or placement adjustments that support a shorter Overview viewport."
  key_links:
    - from: "app/page.tsx"
      to: "src/components/pm-dashboard-tabs.tsx"
      via: "Routes existing result, cost, and forecast outputs into `Обзор` and `Направления` panels without re-homing domain logic"
      pattern: "useState|costResult|scenarioForecast|selectedTab"
    - from: "app/page.tsx"
      to: "src/components/requirement-decision-card.tsx"
      via: "Passes current and forecast summary values derived from existing helpers"
      pattern: "costResult|profitability|scenarioForecast"
    - from: "src/components/direction-breakdown.tsx"
      to: "src/components/dashboard-status.ts"
      via: "Applies the same 20% threshold semantics to full direction rows"
      pattern: "row|status|marginPercent"
---

<objective>
Recompose the requirement page into the PM dashboard while preserving the existing Phase 4 source-of-truth model.

Purpose: deliver the compact decision-first PM screen, move dense direction diagnostics out of Overview, and keep presentational changes separate from current snapshot/scenario/costing logic in `app/page.tsx`.
Output: a wired dashboard shell in the page, a dense `Направления` view, and final desktop verification of scanability.
</objective>

<execution_context>
@/Users/alexanderseleznev/.codex/get-shit-done/workflows/execute-plan.md
@/Users/alexanderseleznev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-pm-dashboard/05-CONTEXT.md
@.planning/phases/05-pm-dashboard/05-RESEARCH.md
@.planning/phases/04-scenario-editing/04-03-SUMMARY.md
@app/page.tsx
@src/components/pm-dashboard-tabs.tsx
@src/components/requirement-decision-card.tsx
@src/components/direction-breakdown.tsx
@src/components/scenario-origin-panel.tsx
@src/lib/costing/calculateProfitability.ts
@src/lib/scenario/forecast.ts

<interfaces>
From src/lib/costing/types.ts:
```ts
export type DirectionCostTotals = {
  readonly role: CostDirectionKey;
  readonly minutes: number;
  readonly hours: number;
  readonly cost: number | null;
};

export type DirectionProfitability = {
  readonly role: CostDirectionKey;
  readonly budget: number | null;
  readonly cost: number | null;
  readonly delta: number | null;
  readonly marginPercent: number | null;
  readonly neededUpsell: number | null;
};
```

From src/lib/scenario/types.ts:
```ts
export type ScenarioDirectionDelta = {
  readonly role: CostDirectionKey;
  readonly addedHours: number;
  readonly addedCost: number | null;
};

export type ScenarioForecastResult = {
  readonly directionTotals: ScenarioComparison<readonly DirectionCostTotals[]>;
  readonly directionProfitability: ScenarioComparison<readonly DirectionProfitability[]>;
  readonly directionDeltas: readonly ScenarioDirectionDelta[];
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Recompose `app/page.tsx` around the new PM dashboard shell without changing its data ownership</name>
  <files>app/page.tsx, src/components/scenario-origin-panel.tsx</files>
  <action>Refactor the page composition so the first desktop viewport is the new tab shell with `Обзор` selected by default, per D-01, D-02, D-04, and D-14. Keep `app/page.tsx` as the owner of `result`, `scenarioState`, `costResult`, `profitability`, and `scenarioForecast`; only redistribute where those values are rendered. Wire the new decision card into `Обзор`, keep root issue lookup and scenario-editing controls accessible without letting them dominate the initial Overview viewport, and move or compact provenance so it supports the dashboard instead of recreating the long Phase 4 scroll. Ensure the wired card still displays the required `neededUpsell`/missing amount to reach 20% within the allowed compact structure from Plan 01. Per D-03 and D-12, remove the dense direction breakdown from Overview entirely and route it only through the directions tab. Do not move calculation helpers out of the page, do not change scenario source-of-truth rules, and do not add extra tabs, mobile layouts, or textual overrun explanations.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>The page renders a compact Overview-first PM dashboard while the existing snapshot/scenario/costing logic remains page-owned and behaviorally unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Convert the directions surface into a dense desktop scan view with full-row health semantics</name>
  <files>src/components/direction-breakdown.tsx, app/page.tsx</files>
  <action>Rework `DirectionBreakdown` from the current pair-of-cards pattern into a dense desktop table or row grid inside the `Направления` tab. Per D-03, D-11, D-13, and UI-02, each row must show at least: direction name, budget, actual hours, extra hours, cost, and profitability. Per UI-04, add one explicit contribution cue that helps a PM identify what drags the requirement down fastest, such as delta or `needed to reach 20%`, and keep `unmapped` visible instead of hiding it. Apply full-row color semantics from the shared status helper, preferring the active forecast margin when scenario extra hours exist and the current margin otherwise, so the densest scan surface still honors the locked 20% rule. Do not revert to stacked current/forecast cards, do not duplicate current-vs-forecast formulas in the component, and do not pull this detail back into Overview.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>The directions tab becomes a dense, desktop-first diagnostic surface where risky rows are obvious at a glance and their contribution to the overall profitability drop is legible.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify desktop scanability of the compact PM dashboard</name>
  <files>app/page.tsx, src/components/pm-dashboard-tabs.tsx, src/components/requirement-decision-card.tsx, src/components/direction-breakdown.tsx</files>
  <action>Review the wired desktop dashboard in the browser after the build passes and confirm that the new tab split and density match the locked Phase 05 intent before implementation is considered complete.</action>
  <what-built>Overview-first tab shell, compact top decision card, and dense directions tab wired onto the existing Phase 4 state/calculation model.</what-built>
  <how-to-verify>
    1. Open the requirement page on desktop width and load a successful snapshot with scenario edits available.
    2. Confirm the page opens on `Обзор`, the top decision card is visible before the dense directions view, and the initial viewport no longer reads like the long Phase 4 page.
    3. Confirm the decision card shows three primary blocks (`бюджет`, `трудозатраты`, `рентабельность`) and that any scenario values appear as secondary values inside those blocks instead of separate cards.
    4. Switch to `Направления` and confirm the dense rows show budget, actual hours, extra hours, cost, profitability, and a contribution cue, with full-row red/green semantics that remain readable.
  </how-to-verify>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>The user explicitly approves that the dashboard reads correctly on desktop, or returns concrete layout issues for a follow-up pass.</done>
  <resume-signal>Type `approved` if the dashboard reads correctly on desktop, or describe the layout issue that needs another pass.</resume-signal>
</task>

</tasks>

<verification>
Run `npm run build` after each implementation task. The plan is ready for sign-off only after the desktop walkthrough confirms the first-screen hierarchy and the directions scan surface feel intentional rather than like the old Phase 4 stack moved under tabs.
</verification>

<success_criteria>
Phase 05 ships as a UI composition refactor: the PM sees a compact Overview-first dashboard with a top decision card, the detailed directions surface moves into its own dense desktop tab, full-block and full-row status semantics honor the 20% threshold, and the underlying page-owned scenario/costing model remains intact.
</success_criteria>

<output>
After completion, create `.planning/phases/05-pm-dashboard/05-02-SUMMARY.md`
</output>
