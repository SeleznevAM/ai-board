# Phase 5: PM Dashboard - Context

**Gathered:** 2026-04-16
**Status:** Ready for planning

<domain>
## Phase Boundary

Фаза превращает уже работающий profitability workspace в компактный PM dashboard для одного требования. На выходе пользователь должен быстро увидеть итог по требованию, цветовой статус рентабельности и при необходимости перейти во вкладку с разбивкой по направлениям, не теряясь в деталях на первом экране.

</domain>

<decisions>
## Implementation Decisions

### Page structure
- **D-01:** Страница требования должна использовать tab-based layout.
- **D-02:** Первая вкладка должна быть вкладкой `Обзор`.
- **D-03:** Детальная разбивка по направлениям должна быть вынесена из главного экрана в отдельную вкладку страницы.

### Overview tab
- **D-04:** Вкладка `Обзор` должна быть компактной.
- **D-05:** В верхней части `Обзора` должна находиться главная итоговая плитка требования.
- **D-06:** В главной плитке должны быть 3 основных блока: `бюджет`, `трудозатраты`, `процент рентабельности`.

### Scenario values in overview
- **D-07:** Если сценарные дополнительные часы отсутствуют, в главной плитке показываются только основные значения.
- **D-08:** Если сценарные дополнительные часы есть, в блоке `трудозатраты` справа снизу должно показываться дополнительное значение.
- **D-09:** Если сценарные дополнительные часы есть, в блоке `рентабельности` справа снизу должно показываться прогнозное значение.
- **D-10:** Текущая идея размещать прогнозную рентабельность справа снизу в блоке рентабельности принята как текущее предпочтение, но не как абсолютно жесткое решение, если в planning/research найдется заметно более читаемая подача без нарушения общей структуры.

### Direction tab
- **D-11:** Вкладка направлений нужна для детальной разбивки по `backend`, `qa`, `analytics` и другим направлениям.
- **D-12:** На главной вкладке не нужно отдельное объяснение причин перерасхода; для детализации пользователь переходит во вкладку направлений.

### Visual emphasis
- **D-13:** Цветовая индикация должна окрашивать весь блок или всю строку, а не только число процента.

### Target device
- **D-14:** Phase 5 проектируется как desktop-only experience.

### the agent's Discretion
- Конкретная визуальная форма вкладок, если она сохраняет быстрый desktop workflow.
- Точная композиция второй вкладки с разбивкой по направлениям, если она не возвращает лишнюю перегруженность на первый экран.
- Детали цветовой шкалы внутри красного/зеленого статуса, если сохраняется понятная семантика порога `20%`.

</decisions>

<specifics>
## Specific Ideas

- Пользователь хочет, чтобы первый экран был максимально быстрым для PM: сначала итог по требованию, а уже потом детализация.
- Главная плитка должна работать как decision card: бюджет, трудозатраты, рентабельность — в одном месте и без длинного аналитического полотна.
- Разбивка по направлениям нужна, но не должна конкурировать с главным итогом на первом экране.

</specifics>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Project scope
- `.planning/PROJECT.md` — общий продуктовый контекст после завершения Phase 4.
- `.planning/REQUIREMENTS.md` — требования `UI-01..04` и уже закрытые зависимости из предыдущих фаз.
- `.planning/ROADMAP.md` — goal и success criteria для Phase 5.
- `.planning/STATE.md` — текущее состояние проекта после завершения Phase 4.

### Prior phase artifacts
- `.planning/phases/03-cost-engine/03-CONTEXT.md` — baseline экономической модели и разбиения по направлениям.
- `.planning/phases/04-scenario-editing/04-CONTEXT.md` — сценарные правила и current-vs-forecast semantics.
- `.planning/phases/04-scenario-editing/04-UI-SPEC.md` — уже утвержденные визуальные и interaction contracts для scenario workspace.
- `.planning/phases/04-scenario-editing/04-VERIFICATION.md` — что уже подтверждено про current-vs-forecast presentation и provenance.

### Existing code
- `app/page.tsx` — текущая страница требования, которую нужно превратить в compact PM dashboard.
- `src/components/cost-summary.tsx` — текущий summary block current-vs-forecast метрик.
- `src/components/direction-breakdown.tsx` — текущая детальная разбивка по направлениям, которую нужно вынести во вкладку.
- `src/components/scenario-origin-panel.tsx` — текущий provenance block, который может повлиять на плотность Overview.
- `src/components/scope-tree.tsx` — текущее дерево задач, если planner решит оставить его доступным, но не доминирующим.

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- Уже есть рабочие summary и direction breakdown компоненты, которые можно перестроить в dashboard, а не писать с нуля.
- Уже есть current-vs-forecast semantics и scenario provenance, доказанные в Phase 4.

### Established Patterns
- Проект уже закрепил, что факт и прогноз должны показываться одновременно и не смешиваться.
- Сценарный слой уже живет поверх канонического snapshot, поэтому Phase 5 не должна ломать эту границу ради UI-компоновки.
- Предыдущие фазы строили один requirement-centric workspace, так что dashboard тоже должен оставаться centered around one requirement.

### Integration Points
- Phase 5 будет опираться на `app/page.tsx` как на существующую точку сборки.
- Нужно перераспределить текущие блоки между вкладкой `Обзор` и вкладкой направлений.
- Нужно добавить visual emphasis на threshold `20%` на уровне целых блоков/строк.

</code_context>

<deferred>
## Deferred Ideas

- Мобильная оптимизация и mobile-first layout — вне границ Phase 5.
- Автоматическое текстовое объяснение причин перерасхода — не нужно в этой фазе.
- Дополнительные вкладки сверх `Обзор` и направления — отложено, если не появится новая явная продуктовая потребность.

</deferred>

---
*Phase: 05-pm-dashboard*
*Context gathered: 2026-04-16*
