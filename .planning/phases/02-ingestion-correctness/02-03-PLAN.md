---
phase: 02-ingestion-correctness
plan: "03"
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - src/components/root-issue-form.tsx
  - src/components/scope-tree.tsx
  - src/components/scope-state.tsx
  - app/page.tsx
autonomous: true
requirements:
  - YTSC-04
  - WORK-03
user_setup: []
must_haves:
  truths:
    - "The user can manually refresh a requirement snapshot and see when the last successful sync occurred"
    - "Blocked snapshots highlight unestimated tasks inside the tree and list them in a dismissible top-right error surface"
    - "Problematic tree nodes link directly back to the corresponding YouTrack issue"
  artifacts:
    - path: "src/components/root-issue-form.tsx"
      provides: "Manual refresh UI that surfaces snapshot freshness"
      min_lines: 60
      contains: "Last sync"
    - path: "src/components/scope-tree.tsx"
      provides: "Tree renderer with problem-state highlighting and issue links"
      min_lines: 70
      contains: "https://"
    - path: "src/components/scope-state.tsx"
      provides: "Dismissible blocked-snapshot state listing unestimated issues"
      min_lines: 60
      contains: "dismiss"
  key_links:
    - from: "src/components/root-issue-form.tsx"
      to: "app/api/scope/route.ts"
      via: "manual refresh submits to the snapshot route"
      pattern: "/api/scope"
    - from: "app/page.tsx"
      to: "src/components/scope-tree.tsx"
      via: "page renders highlighted tree nodes after refresh"
      pattern: "ScopeTree"
    - from: "app/page.tsx"
      to: "src/components/scope-state.tsx"
      via: "page renders dismissible blocked snapshot state"
      pattern: "ScopeState"
---

<objective>
Deliver the user-visible refresh and blocked-snapshot experience for phase two.

Purpose: Let the user refresh YouTrack data, see freshness, and act on blocked issues directly from the tree and error surface.
Output: Refresh UI, last-sync feedback, highlighted missing-estimate nodes, and dismissible blocked-state presentation.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-ingestion-correctness/02-CONTEXT.md
@.planning/phases/02-ingestion-correctness/02-RESEARCH.md
@app/page.tsx
@src/components/root-issue-form.tsx
@src/components/scope-tree.tsx
@src/components/scope-state.tsx
@app/api/scope/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add manual refresh feedback and last-sync presentation</name>
  <files>src/components/root-issue-form.tsx, app/page.tsx</files>
  <read_first>src/components/root-issue-form.tsx, app/page.tsx, app/api/scope/route.ts, .planning/phases/02-ingestion-correctness/02-CONTEXT.md</read_first>
  <action>Update the root issue flow so manual refresh explicitly requests a new snapshot and surfaces the `syncedAt` timestamp of the latest successful refresh. Render this freshness information on the page in a clear but lightweight way, preserving the existing phase-one flow while making the new manual-refresh behavior obvious to the user.</action>
  <verify>grep -q "Last sync" src/components/root-issue-form.tsx && grep -q "syncedAt" app/page.tsx && grep -q "/api/scope" src/components/root-issue-form.tsx</verify>
  <acceptance_criteria>
    - `src/components/root-issue-form.tsx` contains `Last sync`
    - `src/components/root-issue-form.tsx` contains `/api/scope`
    - `app/page.tsx` contains `syncedAt`
    - `app/page.tsx` contains refresh-specific UI copy
  </acceptance_criteria>
  <done>The page exposes the manual refresh action and visible freshness state required by Phase 2.</done>
</task>

<task type="auto">
  <name>Task 2: Render blocked snapshot issues in the tree and dismissible error surface</name>
  <files>src/components/scope-tree.tsx, src/components/scope-state.tsx, app/page.tsx</files>
  <read_first>src/components/scope-tree.tsx, src/components/scope-state.tsx, app/page.tsx, .planning/phases/02-ingestion-correctness/02-CONTEXT.md, .planning/phases/02-ingestion-correctness/02-RESEARCH.md</read_first>
  <action>Update `src/components/scope-tree.tsx` so unestimated issues are highlighted with a soft red treatment and include a direct YouTrack link by readable key. Update `src/components/scope-state.tsx` so blocked snapshots render a dismissible top-right error card listing all unestimated issues. Wire `app/page.tsx` so blocked snapshot responses show the problem list and tree highlights together, while successful snapshots continue rendering without the blocked error surface.</action>
  <verify>grep -q "dismiss" src/components/scope-state.tsx && grep -q "target=\"_blank\"" src/components/scope-tree.tsx && grep -q "ScopeState" app/page.tsx</verify>
  <acceptance_criteria>
    - `src/components/scope-tree.tsx` contains `target="_blank"`
    - `src/components/scope-tree.tsx` contains a soft red problem-state style
    - `src/components/scope-state.tsx` contains `dismiss`
    - `src/components/scope-state.tsx` contains blocked issue list rendering
    - `app/page.tsx` contains `ScopeState`
    - `app/page.tsx` contains the blocked snapshot integration path
  </acceptance_criteria>
  <done>The user can see, dismiss, and act on blocked ingestion issues directly from the page.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `grep -q "Last sync" src/components/root-issue-form.tsx`
- [ ] `grep -q "dismiss" src/components/scope-state.tsx`
- [ ] `grep -q "target=\"_blank\"" src/components/scope-tree.tsx`
- [ ] `npm run build`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The user can see freshness and blocked-ingestion issues without leaving the current page
- The tree remains the primary diagnostic surface, with links back to YouTrack for resolution
  </success_criteria>

<output>
After completion, create `.planning/phases/02-ingestion-correctness/02-03-SUMMARY.md`
</output>
