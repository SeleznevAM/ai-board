---
phase: 02-ingestion-correctness
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/youtrack/contracts.ts
  - src/lib/youtrack/client.ts
  - src/lib/ingestion/types.ts
  - src/lib/ingestion/normalizeIssueHours.ts
  - src/lib/ingestion/normalizeIssueHours.test.ts
autonomous: true
requirements:
  - WORK-01
  - WORK-02
user_setup: []
must_haves:
  truths:
    - "Phase 2 chooses issue hours by the company status model, not by a single universal source field"
    - "Unexpected statuses fall back to estimate instead of blocking the snapshot immediately"
    - "Normalization keeps canonical issue IDs so later aggregation cannot double-count by shape drift"
  artifacts:
    - path: "src/lib/ingestion/types.ts"
      provides: "Canonical snapshot and blocked-state types for phase-two ingestion"
      min_lines: 40
      contains: "SnapshotIssueNode"
    - path: "src/lib/ingestion/normalizeIssueHours.ts"
      provides: "Status-based hours normalization for a single issue"
      min_lines: 80
      contains: "export function normalizeIssueHours"
    - path: "src/lib/ingestion/normalizeIssueHours.test.ts"
      provides: "Regression coverage for spent-time vs estimate policy"
      min_lines: 80
      contains: "unexpected status"
  key_links:
    - from: "src/lib/ingestion/normalizeIssueHours.ts"
      to: "src/lib/youtrack/contracts.ts"
      via: "normalization reads YouTrack field shapes"
      pattern: "YouTrack"
    - from: "src/lib/ingestion/normalizeIssueHours.ts"
      to: "src/lib/ingestion/types.ts"
      via: "normalization returns canonical snapshot issue shape"
      pattern: "SnapshotIssueNode"
---

<objective>
Create the phase-two normalization contract.

Purpose: Lock the status-to-hours-source policy in code before building refresh or UI behavior.
Output: Canonical ingestion types, status-based normalization logic, and unit tests covering the company status model and fallback rules.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-ingestion-correctness/02-CONTEXT.md
@.planning/phases/02-ingestion-correctness/02-RESEARCH.md
@.planning/phases/02-ingestion-correctness/02-VALIDATION.md
@src/lib/youtrack/contracts.ts
@src/lib/youtrack/client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the YouTrack contract for phase-two hours fields</name>
  <files>src/lib/youtrack/contracts.ts, src/lib/youtrack/client.ts, src/lib/ingestion/types.ts</files>
  <read_first>src/lib/youtrack/contracts.ts, src/lib/youtrack/client.ts, .planning/phases/02-ingestion-correctness/02-CONTEXT.md, .planning/phases/02-ingestion-correctness/02-RESEARCH.md</read_first>
  <action>Add the field contract needed for phase-two ingestion: readable status value, estimate value, spent-time value, and any canonical issue metadata needed for refresh snapshots. Create `src/lib/ingestion/types.ts` with snapshot node types that preserve issue identity, hours source, normalized hours, and blocked metadata hooks for later plans. Update the existing YouTrack client field selection and payload mapping only as far as needed to support these new phase-two fields without breaking phase-one tree resolution.</action>
  <verify>grep -q "SnapshotIssueNode" src/lib/ingestion/types.ts && grep -q "estimate" src/lib/youtrack/contracts.ts && grep -q "spent" src/lib/youtrack/contracts.ts</verify>
  <acceptance_criteria>
    - `src/lib/ingestion/types.ts` contains `SnapshotIssueNode`
    - `src/lib/ingestion/types.ts` contains `hoursSource`
    - `src/lib/youtrack/contracts.ts` contains `estimate`
    - `src/lib/youtrack/contracts.ts` contains `spent`
    - `src/lib/youtrack/client.ts` contains the phase-two fields in its issue request contract
  </acceptance_criteria>
  <done>Phase 2 has a stable field and type contract for snapshot normalization.</done>
</task>

<task type="auto">
  <name>Task 2: Implement status-based hours normalization with regression coverage</name>
  <files>src/lib/ingestion/normalizeIssueHours.ts, src/lib/ingestion/normalizeIssueHours.test.ts</files>
  <read_first>src/lib/ingestion/types.ts, src/lib/youtrack/contracts.ts, .planning/phases/02-ingestion-correctness/02-CONTEXT.md, .planning/phases/02-ingestion-correctness/02-VALIDATION.md</read_first>
  <action>Create `src/lib/ingestion/normalizeIssueHours.ts` with `normalizeIssueHours` that maps company closed statuses (`на проверке QA`, `сделана`, `утверждена`) to spent time, active statuses (`зарегистрирована`, `открыта`, `в работе`, `код ревью`) to estimate, unexpected statuses to estimate, and marks the issue blocked when estimate is required but missing. Add `src/lib/ingestion/normalizeIssueHours.test.ts` covering every approved status group, missing-estimate blocking, and unexpected-status fallback so later plans can rely on this logic as a source of truth.</action>
  <verify>grep -q "export function normalizeIssueHours" src/lib/ingestion/normalizeIssueHours.ts && grep -q "unexpected status" src/lib/ingestion/normalizeIssueHours.test.ts && npm run test -- ingestion</verify>
  <acceptance_criteria>
    - `src/lib/ingestion/normalizeIssueHours.ts` contains `export function normalizeIssueHours`
    - `src/lib/ingestion/normalizeIssueHours.ts` contains `на проверке QA`
    - `src/lib/ingestion/normalizeIssueHours.ts` contains `код ревью`
    - `src/lib/ingestion/normalizeIssueHours.test.ts` contains `unexpected status`
    - `src/lib/ingestion/normalizeIssueHours.test.ts` contains `missing estimate`
  </acceptance_criteria>
  <done>The codebase has a tested normalization policy for issue hours that exactly matches the company status model.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `grep -q "SnapshotIssueNode" src/lib/ingestion/types.ts`
- [ ] `grep -q "export function normalizeIssueHours" src/lib/ingestion/normalizeIssueHours.ts`
- [ ] `grep -q "unexpected status" src/lib/ingestion/normalizeIssueHours.test.ts`
- [ ] `npm run test -- ingestion`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The status model is encoded once and can be reused by later refresh and UI layers
- Missing estimate is represented as a blocking normalization result, not a fake zero
  </success_criteria>

<output>
After completion, create `.planning/phases/02-ingestion-correctness/02-01-SUMMARY.md`
</output>
