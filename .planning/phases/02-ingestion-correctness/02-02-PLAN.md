---
phase: 02-ingestion-correctness
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/lib/ingestion/buildRequirementSnapshot.ts
  - src/lib/ingestion/buildRequirementSnapshot.test.ts
  - app/api/scope/route.ts
  - src/lib/scope/types.ts
autonomous: true
requirements:
  - YTSC-04
  - WORK-01
  - WORK-02
  - WORK-03
user_setup: []
must_haves:
  truths:
    - "Manual refresh performs a full snapshot rebuild from the root issue rather than a partial patch"
    - "Aggregation sums normalized hours across the canonical tree without double-counting"
    - "Blocked issues suppress successful snapshot output and return structured problem details"
  artifacts:
    - path: "src/lib/ingestion/buildRequirementSnapshot.ts"
      provides: "Full requirement refresh pipeline and aggregation logic"
      min_lines: 100
      contains: "export async function buildRequirementSnapshot"
    - path: "src/lib/ingestion/buildRequirementSnapshot.test.ts"
      provides: "Regression coverage for mixed-source aggregation and blocked snapshots"
      min_lines: 100
      contains: "full refresh"
    - path: "app/api/scope/route.ts"
      provides: "Manual refresh route returning timestamped snapshot state"
      exports:
        - "POST"
  key_links:
    - from: "src/lib/ingestion/buildRequirementSnapshot.ts"
      to: "src/lib/scope/buildScopeTree.ts"
      via: "refresh starts from the phase-one canonical tree"
      pattern: "buildScopeTree"
    - from: "app/api/scope/route.ts"
      to: "src/lib/ingestion/buildRequirementSnapshot.ts"
      via: "route returns snapshot payloads built by the refresh pipeline"
      pattern: "buildRequirementSnapshot"
---

<objective>
Implement the full refresh and aggregation path for phase two.

Purpose: Turn the normalization contract into a complete, timestamped requirement snapshot that the UI can trust or explicitly block.
Output: Refresh pipeline, aggregation tests, and route payloads that distinguish successful snapshots from blocked results.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-ingestion-correctness/02-CONTEXT.md
@.planning/phases/02-ingestion-correctness/02-RESEARCH.md
@src/lib/scope/buildScopeTree.ts
@src/lib/scope/types.ts
@app/api/scope/route.ts
@src/lib/ingestion/types.ts
@src/lib/ingestion/normalizeIssueHours.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the full requirement snapshot pipeline</name>
  <files>src/lib/ingestion/buildRequirementSnapshot.ts, src/lib/scope/types.ts</files>
  <read_first>src/lib/scope/buildScopeTree.ts, src/lib/scope/types.ts, src/lib/ingestion/types.ts, src/lib/ingestion/normalizeIssueHours.ts, .planning/phases/02-ingestion-correctness/02-CONTEXT.md</read_first>
  <action>Create `src/lib/ingestion/buildRequirementSnapshot.ts` with `buildRequirementSnapshot` that starts from the phase-one canonical tree, resolves phase-two fields for every issue in the tree, normalizes each issue's hours, aggregates total normalized hours without double-counting, records a snapshot timestamp, and returns either a complete snapshot or a blocked snapshot result containing the unestimated issues. Extend `src/lib/scope/types.ts` only as needed so the route and UI can distinguish snapshot-ready states from blocked-ingestion states without breaking the phase-one tree contract.</action>
  <verify>grep -q "export async function buildRequirementSnapshot" src/lib/ingestion/buildRequirementSnapshot.ts && grep -q "syncedAt" src/lib/ingestion/buildRequirementSnapshot.ts && grep -q "blockedIssues" src/lib/ingestion/buildRequirementSnapshot.ts</verify>
  <acceptance_criteria>
    - `src/lib/ingestion/buildRequirementSnapshot.ts` contains `export async function buildRequirementSnapshot`
    - `src/lib/ingestion/buildRequirementSnapshot.ts` contains `syncedAt`
    - `src/lib/ingestion/buildRequirementSnapshot.ts` contains `blockedIssues`
    - `src/lib/ingestion/buildRequirementSnapshot.ts` contains canonical aggregation keyed by issue id
  </acceptance_criteria>
  <done>The backend can produce a full or blocked requirement snapshot from a full refresh run.</done>
</task>

<task type="auto">
  <name>Task 2: Expose manual refresh through the route with aggregation regression tests</name>
  <files>src/lib/ingestion/buildRequirementSnapshot.test.ts, app/api/scope/route.ts</files>
  <read_first>src/lib/ingestion/buildRequirementSnapshot.ts, app/api/scope/route.ts, src/lib/youtrack/errors.ts, .planning/REQUIREMENTS.md</read_first>
  <action>Create `src/lib/ingestion/buildRequirementSnapshot.test.ts` covering mixed spent-time/estimate aggregation, blocked snapshots when estimate is missing, and full refresh behavior that rebuilds from the root issue. Update `app/api/scope/route.ts` so the manual refresh path returns the timestamped snapshot payload on success, includes blocked issue data when ingestion is invalid, and never emits a misleading success state for blocked snapshots. Preserve existing phase-one root issue error semantics.</action>
  <verify>grep -q "buildRequirementSnapshot" app/api/scope/route.ts && grep -q "blockedIssues" app/api/scope/route.ts && npm run test -- ingestion</verify>
  <acceptance_criteria>
    - `src/lib/ingestion/buildRequirementSnapshot.test.ts` contains `full refresh`
    - `src/lib/ingestion/buildRequirementSnapshot.test.ts` contains `mixed spent and estimate`
    - `src/lib/ingestion/buildRequirementSnapshot.test.ts` contains `missing estimate`
    - `app/api/scope/route.ts` contains `buildRequirementSnapshot`
    - `app/api/scope/route.ts` contains `syncedAt`
    - `app/api/scope/route.ts` contains `blockedIssues`
  </acceptance_criteria>
  <done>The route exposes a trustworthy manual refresh contract with regression coverage for the most important snapshot behaviors.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `grep -q "export async function buildRequirementSnapshot" src/lib/ingestion/buildRequirementSnapshot.ts`
- [ ] `grep -q "buildRequirementSnapshot" app/api/scope/route.ts`
- [ ] `grep -q "missing estimate" src/lib/ingestion/buildRequirementSnapshot.test.ts`
- [ ] `npm run test -- ingestion`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Manual refresh rebuilds the requirement snapshot from the root each time
- Aggregation cannot silently double-count nodes or emit false-success data
  </success_criteria>

<output>
After completion, create `.planning/phases/02-ingestion-correctness/02-02-SUMMARY.md`
</output>
