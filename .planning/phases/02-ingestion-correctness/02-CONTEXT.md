# Phase 2: Ingestion Correctness - Context

**Gathered:** 2026-04-08
**Status:** Ready for planning

<domain>
## Phase Boundary

Фаза определяет, как система делает полный переснимок одного требования из YouTrack и получает достоверный снимок часов по всему дереву задач. На выходе пользователь должен иметь возможность вручную обновить данные из YouTrack, увидеть актуальный срез по требованию и понять, можно ли доверять расчету или он заблокирован из-за неоцененных задач.

</domain>

<decisions>
## Implementation Decisions

### Refresh model
- **D-01:** При ручном обновлении система должна делать полный переснимок требования из YouTrack с нуля, а не частично обновлять отдельные ветки дерева.

### Status-based hours policy
- **D-02:** Источник часов зависит от статуса задачи.
- **D-03:** Если задача находится в статусах `на проверке QA`, `сделана`, `утверждена`, она считается закрытой и для нее нужно брать поле `затраченное время`.
- **D-04:** Если задача находится в статусах `зарегистрирована`, `открыта`, `в работе`, `код ревью`, для нее нужно брать поле `оценка`.
- **D-05:** Если у задачи неожиданный статус, для нее тоже нужно брать поле `оценка`.
- **D-06:** В одном дереве требования допустимо суммировать задачи, часть которых считается по `затраченному времени`, а часть по `оценке`.

### Incomplete data behavior
- **D-07:** Если у задачи нет оценки в том случае, когда по ее статусу должна использоваться оценка, расчет не должен выполняться.
- **D-08:** Нужное поле в модели YouTrack считаем существующим всегда; проблема для блокировки расчета — это отсутствие значения, а не отсутствие самого поля.

### UI for blocked calculation
- **D-09:** Задача без оценки должна быть выделена в дереве мягким красным цветом, без агрессивного визуального акцента.
- **D-10:** Справа сверху должно показываться закрываемое сообщение о том, какие задачи не оценены.
- **D-11:** В сообщении об ошибке должен быть перечислен список неоцененных задач.
- **D-12:** В карточке проблемной задачи в дереве должна быть ссылка на задачу в YouTrack.
- **D-13:** Ссылка на задачу должна вести прямо на задачу в YouTrack по `readable key`.

### the agent's Discretion
- Точная форма хранения snapshot-данных после переснимка, если она не меняет пользовательскую семантику фазы.
- Конкретный визуальный тон красной подсветки и расположение toast/error-блока при соблюдении условия "не вырвиглазно".
- Внутренний способ нормализации статусов YouTrack, если он не меняет зафиксированные правила выбора поля часов.

</decisions>

<specifics>
## Specific Ideas

- Эта фаза не просто про импорт worklog-ов: она должна уметь собирать единый расчетный срез по статусной модели компании.
- Для доверия к цифре пользователь предпочел жесткую логику: если задача должна считаться по оценке, но не оценена, расчет нужно блокировать, а не занижать, не пропускать и не подставлять ноль.
- Ошибка по неоцененным задачам должна быть одновременно заметной и рабочей: пользователь видит и верхнее сообщение, и саму проблемную задачу в дереве, и может перейти по ссылке в YouTrack.

</specifics>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Project scope
- `.planning/PROJECT.md` — общий продуктовый контекст, ценность продукта и ограничения v1.
- `.planning/REQUIREMENTS.md` — требования `YTSC-04`, `WORK-01`, `WORK-02`, `WORK-03` и phase mapping.
- `.planning/ROADMAP.md` — границы Phase 2 и success criteria по полноте и достоверности snapshot.
- `.planning/STATE.md` — текущее состояние проекта после завершения Phase 1.

### Prior phase artifacts
- `.planning/phases/01-youtrack-data-contract/01-CONTEXT.md` — зафиксированные правила scope и видимости дерева.
- `.planning/phases/01-youtrack-data-contract/01-VERIFICATION.md` — что уже доказано и считается устойчивым после Phase 1.

### Existing code
- `app/api/scope/route.ts` — текущий контракт route для scope discovery.
- `src/lib/scope/buildScopeTree.ts` — текущая логика обхода дерева задач.
- `src/lib/youtrack/client.ts` — базовый слой запросов к YouTrack, который уже работает на реальных данных.
- `src/components/scope-tree.tsx` — текущее представление дерева задач, в которое позже будет встроена индикация проблемных задач.

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- Уже есть рабочий route и клиент для доступа к YouTrack, плюс подтвержденный обход иерархии `parent/subtask`.
- Уже есть пользовательский экран с деревом задач и раздельными blocked/error states.

### Established Patterns
- Phase 1 закрепила, что система должна работать по полному дереву требования, а не по частичному результату.
- YouTrack integration уже идет через `src/lib/youtrack/client.ts`, поэтому Phase 2 должна развивать этот слой, а не дублировать его в UI или route-обработчиках.

### Integration Points
- Phase 2 должна использовать готовый scope-контракт из Phase 1 как вход.
- Данные этой фазы позже станут источником для cost engine, поэтому snapshot должен быть объяснимым и пригодным для детерминированного пересчета.

</code_context>

<deferred>
## Deferred Ideas

- Подробная explainability до отдельных worklog-записей — это уже больше похоже на будущие explainability/v2-задачи.
- Настройка статусной модели пользователем в интерфейсе — вне границ текущей фазы, сейчас статусы фиксированы решениями выше.
- Частичное обновление дерева вместо полного переснимка — сознательно не выбрано для v1.

</deferred>

---
*Phase: 02-ingestion-correctness*
*Context gathered: 2026-04-08*
