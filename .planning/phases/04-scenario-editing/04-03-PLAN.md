---
phase: 04-scenario-editing
plan: "03"
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - app/page.tsx
  - src/components/cost-summary.tsx
  - src/components/direction-breakdown.tsx
  - src/components/scope-state.tsx
  - src/components/scenario-origin-panel.tsx
autonomous: true
requirements: [BUDG-02, REFO-02, REFO-03, AUDT-01, AUDT-02]
must_haves:
  truths:
    - "When a scenario exists, the user can see current and forecast cost/profitability at the same time."
    - "Direction rows show actual hours, extra hours, forecast cost, and forecast profitability without hiding unchanged directions."
    - "The current screen explains calculation provenance with root issue, last sync, baseline budgets, scenario budgets, and extra hours, without persistence."
  artifacts:
    - path: "src/components/cost-summary.tsx"
      provides: "Current-vs-forecast top-level metrics with target-margin deficit output."
    - path: "src/components/direction-breakdown.tsx"
      provides: "Per-direction actual and forecast figures in one surface."
    - path: "src/components/scenario-origin-panel.tsx"
      provides: "Minimal provenance panel for root issue, last sync, and overlay inputs."
    - path: "app/page.tsx"
      provides: "Final composition of summary, breakdown, provenance, and blocked-state gating."
  key_links:
    - from: "app/page.tsx"
      to: "src/lib/scenario/forecast.ts"
      via: "Feeds factual and forecast outputs into presentation components"
      pattern: "forecast|current"
    - from: "src/components/cost-summary.tsx"
      to: "src/lib/costing/calculateProfitability.ts"
      via: "Renders current and forecast profitability without mixing labels"
      pattern: "Needed to reach 20%"
    - from: "src/components/scenario-origin-panel.tsx"
      to: "src/lib/scenario/state.ts"
      via: "Displays in-memory provenance metadata only"
      pattern: "rootIssue|lastSyncedAt|baselineBudgets|scenarioBudgets|extraHoursByIssueKey"
---

<objective>
Present the scenario outputs and provenance so PMs can compare fact and forecast without losing trust in the source data.

Purpose: complete the Phase 4 user outcome by showing current versus forecast metrics and the minimal audit context that explains them.
Output: current-vs-forecast summary, current-vs-forecast direction breakdown, provenance panel, and blocked-state gating for scenario surfaces.
</objective>

<execution_context>
@/Users/alexanderseleznev/.codex/get-shit-done/workflows/execute-plan.md
@/Users/alexanderseleznev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-scenario-editing/04-CONTEXT.md
@.planning/phases/04-scenario-editing/04-RESEARCH.md
@.planning/phases/04-scenario-editing/04-UI-SPEC.md
@.planning/phases/04-scenario-editing/04-02-SUMMARY.md
@src/lib/scenario/types.ts
@src/lib/scenario/forecast.ts
@src/components/cost-summary.tsx
@src/components/direction-breakdown.tsx
@app/page.tsx

<interfaces>
From src/components/cost-summary.tsx:
```ts
type CostSummaryProps = {
  readonly totalHours: number;
  readonly totalCost: number | null;
  readonly profitability: RequirementProfitability;
};
```

From src/components/direction-breakdown.tsx:
```ts
type DirectionBreakdownProps = {
  readonly directionTotals: readonly DirectionCostTotals[];
  readonly profitability: readonly DirectionProfitability[];
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render current and forecast metrics side by side in the summary and direction breakdown</name>
  <files>src/components/cost-summary.tsx, src/components/direction-breakdown.tsx, app/page.tsx</files>
  <action>Update the presentation components to consume both factual and forecast outputs from the Phase 4 forecast pass. Per D-06, D-07, and D-08, show current and forecast cost, profitability, and needed upsell simultaneously with explicit labels that do not mix actual and scenario values. In the direction breakdown, show actual hours, extra hours, forecast cost, and forecast profitability in the same row/card for every direction, including unchanged directions with `0.00` extra hours, per the UI-SPEC. Preserve the existing profitability validity rules so null budgets or incomplete costs still avoid fake percentages.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>The summary and direction surfaces clearly compare actual versus forecast values and recompute immediately from scenario inputs without hiding unchanged directions.</done>
</task>

<task type="auto">
  <name>Task 2: Add minimal provenance output and gate scenario presentation on trustworthy snapshot state</name>
  <files>src/components/scenario-origin-panel.tsx, app/page.tsx, src/components/scope-state.tsx</files>
  <action>Create a small provenance component that renders only the Phase 4 minimum audit fields: `Root issue`, `Last sync`, baseline budgets, scenario budgets, and issue-keyed extra hours. Integrate it into `app/page.tsx` near the summary surfaces so the source of each calculation is explainable per AUDT-01 and AUDT-02, while keeping everything in memory only. If the snapshot is blocked or otherwise untrustworthy, keep the existing blocked/error messaging dominant and suppress misleading scenario summary output until the user has a trustworthy result. Do not add persistence, history, or a separate audit trail screen.</action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>The final workspace shows forecast provenance in-context, labels scenario numbers as forecast, and does not present scenario outputs as trustworthy when the underlying snapshot is blocked.</done>
</task>

</tasks>

<verification>
Run `npm run build` after the summary/breakdown rewrite and `npm test` after provenance integration. Then visually confirm that `Current` and `Forecast` stay simultaneously visible and that blocked snapshots do not render misleading forecast totals.
</verification>

<success_criteria>
The page shows current and forecast money side by side, keeps unchanged directions visible, and exposes enough in-memory provenance to explain the numbers without introducing persistence or a new screen.
</success_criteria>

<output>
After completion, create `.planning/phases/04-scenario-editing/04-03-SUMMARY.md`
</output>
