---
phase: 04-scenario-editing
plan: "02"
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - app/page.tsx
  - src/components/root-issue-form.tsx
  - src/components/budget-allocation-form.tsx
  - src/components/scope-tree.tsx
autonomous: true
requirements: [BUDG-01, BUDG-02, BUDG-03, REFO-01, REFO-04, AUDT-01, AUDT-02]
must_haves:
  truths:
    - "The page owns a local scenario overlay that is recreated from the latest successful snapshot and disappears on page reload."
    - "Refreshing YouTrack with dirty scenario edits requires explicit confirmation before those edits are discarded."
    - "Task cards accept extra hours keyed by issueKey, and direction-budget edits remain separate from the baseline budget snapshot."
  artifacts:
    - path: "app/page.tsx"
      provides: "Canonical result orchestration plus page-level ephemeral scenario overlay."
    - path: "src/components/root-issue-form.tsx"
      provides: "Refresh confirmation gate when scenario edits would be discarded."
    - path: "src/components/budget-allocation-form.tsx"
      provides: "Budget editor that can show baseline budgets separately from the current scenario budget state."
    - path: "src/components/scope-tree.tsx"
      provides: "Task-card extra-hours editor with apply/reset controls keyed by issueKey."
  key_links:
    - from: "app/page.tsx"
      to: "src/lib/scenario/state.ts"
      via: "Initializes and updates overlay state from successful refreshes"
      pattern: "create|reset|dirty|scenario"
    - from: "src/components/root-issue-form.tsx"
      to: "app/page.tsx"
      via: "Calls a refresh guard before POST /api/scope"
      pattern: "window.confirm|onBeforeSubmit"
    - from: "src/components/scope-tree.tsx"
      to: "app/page.tsx"
      via: "Commits per-issue extra-hour edits into page-level scenario state"
      pattern: "issueKey|Apply extra hours|Clear scenario"
---

<objective>
Wire the ephemeral scenario overlay into the existing workspace without breaking the canonical refresh flow.

Purpose: let PMs edit scenario budgets and task-card extra hours in one screen while keeping imported YouTrack data immutable and discardable.
Output: page-level scenario state orchestration, confirmed refresh reset, budget baseline/scenario separation, and task-card extra-hours editing.
</objective>

<execution_context>
@/Users/alexanderseleznev/.codex/get-shit-done/workflows/execute-plan.md
@/Users/alexanderseleznev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-scenario-editing/04-CONTEXT.md
@.planning/phases/04-scenario-editing/04-RESEARCH.md
@.planning/phases/04-scenario-editing/04-UI-SPEC.md
@.planning/phases/03-cost-engine/03-03-SUMMARY.md
@.planning/phases/02-ingestion-correctness/02-03-SUMMARY.md
@src/lib/scenario/types.ts
@src/lib/scenario/state.ts
@src/components/root-issue-form.tsx
@src/components/budget-allocation-form.tsx
@src/components/scope-tree.tsx
@app/page.tsx

<interfaces>
From src/components/root-issue-form.tsx:
```ts
type RootIssueFormProps = {
  readonly onResolved: (result: RootIssueFormResult) => void;
  readonly lastSyncedAt?: string;
};
```

From src/components/budget-allocation-form.tsx:
```ts
type BudgetAllocationFormProps = {
  readonly totalBudget: number | null;
  readonly directionBudgets: DirectionBudgetMap;
  readonly onChange: (nextState: {
    totalBudget: number | null;
    directionBudgets: DirectionBudgetMap;
  }) => void;
};
```

From src/components/scope-tree.tsx:
```ts
type ScopeTreeProps = {
  readonly root: ScopeTreeNode;
  readonly blockedIssueKeys?: readonly string[];
  readonly missingAssigneeIssueKeys?: readonly string[];
  readonly youTrackBaseUrl?: string;
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace single budget state with a page-level scenario overlay and refresh discard guard</name>
  <files>app/page.tsx, src/components/root-issue-form.tsx, src/components/budget-allocation-form.tsx</files>
  <action>Update the workspace so `app/page.tsx` owns both the canonical result and the Phase 4 overlay from `src/lib/scenario/state.ts`. On every successful or blocked refresh, materialize a new baseline from the imported budgets and synced root issue context; keep editable scenario budgets separate from the baseline per BUDG-03 and D-01. Allow per-direction scenario budget edits because Phase 4 requirements BUDG-01 and BUDG-02 require them, but keep them clearly secondary to the agreed baseline and do not persist them, per D-02 and D-09. Extend `RootIssueForm` with a pre-submit callback that uses `window.confirm` only when the overlay is dirty and a refresh would discard current scenario edits, per D-10 and the fixed UI-SPEC copy. Do not add `beforeunload`, localStorage, or any new API route.</action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>The main page can track baseline budgets separately from scenario budgets, rebuild the overlay from the latest refresh, and block destructive refreshes until the user confirms.</done>
</task>

<task type="auto">
  <name>Task 2: Add task-card scenario editing keyed by issueKey in the existing scope tree</name>
  <files>src/components/scope-tree.tsx, app/page.tsx</files>
  <action>Extend `ScopeTree` task cards with the Phase 4 input contract from the UI-SPEC: one numeric `Extra hours` input plus explicit `Apply extra hours` and `Clear scenario` actions. Commit values into the page-level overlay keyed by `issueKey`, not by direction, per D-03 and REFO-04. Surface task provenance on-card: current assignee label, scenario confirmation line, and derived added-cost hint when pricing is known from the task assignee per D-05. Keep the existing tree hierarchy, blocked issue highlighting, and missing-assignee warnings intact. Do not create a second tree, modal, or direction-only scenario form.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Task cards can apply and clear issue-keyed extra hours against the page-level overlay while preserving the existing scope tree semantics and diagnostics.</done>
</task>

</tasks>

<verification>
Run `npm test` after the page-state wiring, then `npm run build` after task-card integration. During execution, manually sanity-check that refresh confirmation appears only when scenario edits are dirty and not on ordinary typing.
</verification>

<success_criteria>
The existing requirement workspace owns an ephemeral scenario overlay, refreshes are explicitly guarded, and task-card edits become the single source of forecast-hour input without mutating canonical YouTrack results.
</success_criteria>

<output>
After completion, create `.planning/phases/04-scenario-editing/04-02-SUMMARY.md`
</output>
