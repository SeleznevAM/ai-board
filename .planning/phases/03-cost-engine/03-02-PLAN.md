---
phase: 03-cost-engine
plan: "02"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/lib/costing/budgetAllocation.ts
  - src/lib/costing/budgetAllocation.test.ts
  - src/lib/costing/calculateRequirementCost.ts
  - src/lib/costing/calculateRequirementCost.test.ts
  - src/lib/costing/calculateProfitability.ts
  - src/lib/costing/calculateProfitability.test.ts
autonomous: true
requirements:
  - COST-02
  - COST-03
  - PROF-01
  - PROF-03
  - PROF-04
  - PROF-05
  - PROF-06
user_setup: []
must_haves:
  truths:
    - "Costing happens from the canonical phase-two snapshot and never re-fetches YouTrack"
    - "Every issue becomes one ledger row before direction totals are reduced"
    - "Direction budgets come from one total budget via equal distribution plus manual overrides"
    - "Budget validation can withhold percentages without withholding raw cost totals"
  artifacts:
    - path: "src/lib/costing/budgetAllocation.ts"
      provides: "Pure direction-budget allocation from total budget plus manual overrides"
      min_lines: 120
      contains: "allocateEvenBudgets"
    - path: "src/lib/costing/calculateRequirementCost.ts"
      provides: "Pure cost ledger and direction aggregation from snapshot issues plus directory data"
      min_lines: 140
      contains: "calculateRequirementCost"
    - path: "src/lib/costing/calculateProfitability.ts"
      provides: "Pure overall and per-direction profitability math plus target-20%-upsell calculation"
      min_lines: 120
      contains: "calculateNeededUpsell"
    - path: "src/lib/costing/calculateRequirementCost.test.ts"
      provides: "Regression coverage for unmapped totals, missing-assignee fallback, and overall totals"
      min_lines: 120
      contains: "unmapped"
  key_links:
    - from: "src/lib/costing/calculateRequirementCost.ts"
      to: "src/lib/costing/assigneeDirectory.ts"
      via: "cost ledger relies on normalized assignee lookup and fallback rate policy"
      pattern: "resolveAssigneeDirectoryMatch"
    - from: "src/lib/costing/calculateProfitability.ts"
      to: "src/lib/costing/budgetAllocation.ts"
      via: "per-direction profitability relies on the materialized direction-budget map"
      pattern: "allocateEvenBudgets"
    - from: "src/lib/costing/calculateRequirementCost.ts"
      to: "src/lib/ingestion/types.ts"
      via: "cost engine consumes snapshot issue minutes"
      pattern: "SnapshotIssueNode"
    - from: "src/lib/costing/calculateProfitability.ts"
      to: "src/lib/costing/types.ts"
      via: "profitability returns typed money output for UI reuse"
      pattern: "RequirementProfitability"
---

<objective>
Build the pure money engine for Phase 3.

Purpose: Convert trusted snapshot hours into cost totals, direction totals, direction-budget allocation, and profitability math before wiring UI.
Output: Deterministic budget-allocation, cost, and profitability helpers with unit coverage for unmapped, invalid-budget, per-direction profitability, and target-20% behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-cost-engine/03-CONTEXT.md
@.planning/phases/03-cost-engine/03-RESEARCH.md
@.planning/phases/03-cost-engine/03-VALIDATION.md
@src/lib/ingestion/buildRequirementSnapshot.ts
@src/lib/costing/types.ts
@src/lib/costing/assigneeDirectory.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build direction-budget allocation from total budget plus manual overrides</name>
  <files>src/lib/costing/budgetAllocation.ts, src/lib/costing/budgetAllocation.test.ts</files>
  <read_first>src/lib/costing/types.ts, .planning/phases/03-cost-engine/03-CONTEXT.md, .planning/phases/03-cost-engine/03-RESEARCH.md, .planning/phases/03-cost-engine/03-VALIDATION.md</read_first>
  <action>Create `src/lib/costing/budgetAllocation.ts` with pure helpers that distribute one total budget evenly across the eight core directions `devops`, `backend`, `analytics`, `frontend`, `mobiledev`, `qa`, `design`, `pm`; always seed `unmapped` with `0`; merge manual direction-budget overrides including `unmapped`; and recompute the overall budget as the sum of the direction-budget map after overrides are saved. Add `src/lib/costing/budgetAllocation.test.ts` covering equal distribution, `unmapped` staying `0` in auto-allocation, manual override merge, and total-budget recomputation.</action>
  <verify>grep -q "allocateEvenBudgets" src/lib/costing/budgetAllocation.ts && grep -q "unmapped" src/lib/costing/budgetAllocation.test.ts && npm test</verify>
  <acceptance_criteria>
    - `src/lib/costing/budgetAllocation.ts` contains `allocateEvenBudgets`
    - `src/lib/costing/budgetAllocation.ts` contains the eight core directions
    - `src/lib/costing/budgetAllocation.test.ts` contains `unmapped`
    - `src/lib/costing/budgetAllocation.test.ts` covers total-budget recomputation after override save
  </acceptance_criteria>
  <done>The codebase has a deterministic direction-budget model that matches the updated product rule.</done>
</task>

<task type="auto">
  <name>Task 2: Build the issue ledger and direction cost aggregation</name>
  <files>src/lib/costing/calculateRequirementCost.ts, src/lib/costing/calculateRequirementCost.test.ts</files>
  <read_first>src/lib/costing/types.ts, src/lib/costing/assigneeDirectory.ts, src/lib/ingestion/types.ts, .planning/phases/03-cost-engine/03-RESEARCH.md</read_first>
  <action>Create `calculateRequirementCost` as a pure function that accepts snapshot issues and assignee directory state, turns each issue into one ledger row, and reduces the ledger into overall totals plus direction buckets. Hours should be converted from normalized minutes, personal rates should be used when available, and fallback average rate should be applied for missing or unmapped assignees. Preserve explicit warning rows and `unmapped` totals so the UI can explain why some costs could not be assigned to a known direction.</action>
  <verify>grep -q "calculateRequirementCost" src/lib/costing/calculateRequirementCost.ts && grep -q "unmapped" src/lib/costing/calculateRequirementCost.test.ts && npm test</verify>
  <acceptance_criteria>
    - `src/lib/costing/calculateRequirementCost.ts` contains `calculateRequirementCost`
    - `src/lib/costing/calculateRequirementCost.ts` contains direction aggregation
    - `src/lib/costing/calculateRequirementCost.test.ts` contains `unmapped`
    - `src/lib/costing/calculateRequirementCost.test.ts` covers missing assignee fallback
  </acceptance_criteria>
  <done>The project can derive overall and per-direction cost totals from one trusted snapshot.</done>
</task>

<task type="auto">
  <name>Task 3: Implement overall and per-direction profitability math with target-margin deficit handling</name>
  <files>src/lib/costing/calculateProfitability.ts, src/lib/costing/calculateProfitability.test.ts</files>
  <read_first>src/lib/costing/types.ts, src/lib/costing/budgetAllocation.ts, .planning/phases/03-cost-engine/03-CONTEXT.md, .planning/phases/03-cost-engine/03-VALIDATION.md</read_first>
  <action>Create `calculateProfitability` helpers that compute overall and per-direction margin percent, money delta, and the extra amount required to reach the locked 20% target. Use the direction-budget map from `budgetAllocation.ts` so each direction profitability is based on a real budget value, not an inferred share. Invalid overall or direction budgets must withhold percentages instead of returning fake values. Add tests for the locked formula, invalid budget inputs, per-direction profitability, and the inverse target-20% calculation so the later UI can trust the domain output directly.</action>
  <verify>grep -q "calculateNeededUpsell" src/lib/costing/calculateProfitability.ts && grep -q "20" src/lib/costing/calculateProfitability.test.ts && npm test</verify>
  <acceptance_criteria>
    - `src/lib/costing/calculateProfitability.ts` contains `calculateNeededUpsell`
    - `src/lib/costing/calculateProfitability.ts` contains per-direction profitability output
    - `src/lib/costing/calculateProfitability.ts` contains invalid-budget handling
    - `src/lib/costing/calculateProfitability.test.ts` contains target-margin coverage
    - `src/lib/costing/calculateProfitability.test.ts` contains direction-budget coverage
    - `src/lib/costing/calculateProfitability.test.ts` covers empty or zero budget
  </acceptance_criteria>
  <done>The codebase has a tested profitability layer for overall and per-direction budget vs cost decisions.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `grep -q "allocateEvenBudgets" src/lib/costing/budgetAllocation.ts`
- [ ] `grep -q "calculateRequirementCost" src/lib/costing/calculateRequirementCost.ts`
- [ ] `grep -q "calculateNeededUpsell" src/lib/costing/calculateProfitability.ts`
- [ ] `grep -q "unmapped" src/lib/costing/calculateRequirementCost.test.ts`
- [ ] `npm test`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Overall budgets, direction budgets, and per-direction costs come from one pure, deterministic engine
- Profitability output withholds fake percentages when the budget is invalid
  </success_criteria>

<output>
After completion, create `.planning/phases/03-cost-engine/03-02-SUMMARY.md`
</output>
