---
phase: 03-cost-engine
plan: "03"
type: execute
wave: 3
depends_on:
  - "03-02"
files_modified:
  - app/page.tsx
  - app/assignees/page.tsx
  - src/components/scope-tree.tsx
  - src/components/budget-allocation-form.tsx
  - src/components/cost-summary.tsx
  - src/components/direction-breakdown.tsx
  - src/components/assignee-directory-form.tsx
  - src/lib/costing/storage.ts
autonomous: true
requirements:
  - COST-01
  - COST-02
  - COST-03
  - COST-04
  - PROF-02
  - PROF-03
  - PROF-04
  - PROF-05
  - PROF-06
user_setup: []
must_haves:
  truths:
    - "The current snapshot page remains the primary place to see money results after refresh"
    - "Assignee management lives on a separate page but persists locally inside the application"
    - "Tasks without assignee stay visible in-context and do not disappear into summary-only warnings"
  artifacts:
    - path: "app/assignees/page.tsx"
      provides: "Dedicated assignee role/rate management page"
      min_lines: 60
      contains: "Assignee"
    - path: "src/components/assignee-directory-form.tsx"
      provides: "Manual editor for assignee role/rate directory persisted in the app"
      min_lines: 120
      contains: "localStorage"
    - path: "src/components/budget-allocation-form.tsx"
      provides: "Overall-budget input plus expandable manual direction-budget editor"
      min_lines: 120
      contains: "unmapped"
    - path: "src/components/cost-summary.tsx"
      provides: "Overall budget, cost, profitability, and target-20% deficit UI"
      min_lines: 100
      contains: "20%"
    - path: "src/components/direction-breakdown.tsx"
      provides: "Per-direction hours/cost breakdown including unmapped bucket"
      min_lines: 100
      contains: "unmapped"
  key_links:
    - from: "app/page.tsx"
      to: "src/components/budget-allocation-form.tsx"
      via: "main page derives direction budgets from total input and optional manual overrides"
      pattern: "BudgetAllocationForm"
    - from: "app/page.tsx"
      to: "src/components/cost-summary.tsx"
      via: "main page renders overall cost/profitability after snapshot refresh"
      pattern: "CostSummary"
    - from: "app/page.tsx"
      to: "src/components/direction-breakdown.tsx"
      via: "main page renders direction totals beside the existing tree"
      pattern: "DirectionBreakdown"
    - from: "app/assignees/page.tsx"
      to: "src/components/assignee-directory-form.tsx"
      via: "separate route owns manual assignee mapping and rates"
      pattern: "AssigneeDirectoryForm"
    - from: "src/components/scope-tree.tsx"
      to: "src/components/cost-summary.tsx"
      via: "tree warning state stays aligned with money summary warnings"
      pattern: "warning"
---

<objective>
Deliver the user-visible cost engine experience.

Purpose: Let the user manage assignee role/rate mappings, enter a total budget, optionally tune direction budgets, and see cost plus profitability directly on the refreshed requirement page.
Output: Assignee directory route, local persistence, overall-budget plus direction-budget editing, overall/per-direction profitability summary, and missing-assignee highlighting integrated into the existing UI.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-cost-engine/03-CONTEXT.md
@.planning/phases/03-cost-engine/03-RESEARCH.md
@.planning/phases/03-cost-engine/03-VALIDATION.md
@app/page.tsx
@src/components/scope-tree.tsx
@src/lib/costing/calculateRequirementCost.ts
@src/lib/costing/calculateProfitability.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the assignee directory page with local persistence</name>
  <files>app/assignees/page.tsx, src/components/assignee-directory-form.tsx, src/lib/costing/storage.ts</files>
  <read_first>app/page.tsx, .planning/phases/03-cost-engine/03-CONTEXT.md, .planning/phases/03-cost-engine/03-RESEARCH.md, .planning/phases/03-cost-engine/03-VALIDATION.md</read_first>
  <action>Create a dedicated `/assignees` page and a directory editor component that lets the user add, edit, and remove assignee entries with one role and one personal rate per row. Persist this directory through a small storage wrapper so mappings survive reloads in the same browser. Keep the page narrow and operational: it should optimize for quickly fixing unmapped people rather than becoming a general admin panel.</action>
  <verify>grep -q "localStorage" src/components/assignee-directory-form.tsx && grep -q "Assignee" app/assignees/page.tsx && npm test</verify>
  <acceptance_criteria>
    - `app/assignees/page.tsx` contains assignee directory UI shell
    - `src/components/assignee-directory-form.tsx` contains `localStorage`
    - `src/lib/costing/storage.ts` encapsulates persistence helpers
    - directory entries capture assignee label, role, and personal rate
  </acceptance_criteria>
  <done>The user can manage the assignee directory inside the app without external files or manual code edits.</done>
</task>

<task type="auto">
  <name>Task 2: Wire budget input, direction-budget editor, cost summary, and assignee warnings into the main page</name>
  <files>app/page.tsx, src/components/scope-tree.tsx, src/components/budget-allocation-form.tsx, src/components/cost-summary.tsx, src/components/direction-breakdown.tsx</files>
  <read_first>app/page.tsx, src/components/scope-tree.tsx, src/lib/costing/budgetAllocation.ts, src/lib/costing/calculateRequirementCost.ts, src/lib/costing/calculateProfitability.ts, .planning/phases/03-cost-engine/03-CONTEXT.md</read_first>
  <action>Create `src/components/budget-allocation-form.tsx` and extend the main requirement page so a successful snapshot can be combined with the assignee directory, one total budget input, and a separate direction-budget editor. The form must allocate one total budget evenly across `devops`, `backend`, `analytics`, `frontend`, `mobiledev`, `qa`, `design`, `pm`, keep `unmapped` at `0` on auto-allocation, open a manual form that includes `unmapped`, and after save recompute the total budget as the sum of all direction budgets. Render overall budget, cost, profitability, the amount needed to reach 20%, and per-direction profitability. Render a direction breakdown including `unmapped`. Update the tree so tasks with no assignee are highlighted in-context and clearly labeled, while cost totals continue to include them using the documented fallback rate.</action>
  <verify>grep -q "20%" src/components/cost-summary.tsx && grep -q "unmapped" src/components/direction-breakdown.tsx && grep -q "BudgetAllocationForm" app/page.tsx && npm run build</verify>
  <acceptance_criteria>
    - `src/components/cost-summary.tsx` contains `20%`
    - `src/components/direction-breakdown.tsx` contains `unmapped`
    - `src/components/budget-allocation-form.tsx` contains `unmapped`
    - `app/page.tsx` contains the budget input and renders `BudgetAllocationForm`
    - `app/page.tsx` renders `CostSummary`
    - `src/components/scope-tree.tsx` contains missing-assignee warning treatment
    - invalid budget suppresses fake profitability output
    - manual direction-budget save recomputes the total budget
  </acceptance_criteria>
  <done>The refreshed requirement page now shows usable money decisions without leaving the existing workflow.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `grep -q "localStorage" src/components/assignee-directory-form.tsx`
- [ ] `grep -q "unmapped" src/components/budget-allocation-form.tsx`
- [ ] `grep -q "20%" src/components/cost-summary.tsx`
- [ ] `grep -q "unmapped" src/components/direction-breakdown.tsx`
- [ ] `npm test`
- [ ] `npm run build`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The user can maintain assignee mappings/rates, adjust direction budgets, and immediately see cost + profitability on the requirement page
- Missing-assignee and unmapped spend remain visible and actionable rather than hidden
  </success_criteria>

<output>
After completion, create `.planning/phases/03-cost-engine/03-03-SUMMARY.md`
</output>
