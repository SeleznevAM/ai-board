---
phase: 03-cost-engine
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/ingestion/types.ts
  - src/lib/costing/types.ts
  - src/lib/costing/assigneeDirectory.ts
  - src/lib/costing/assigneeDirectory.test.ts
autonomous: true
requirements:
  - COST-01
  - COST-04
user_setup: []
must_haves:
  truths:
    - "Phase 3 does not read roles from YouTrack; assignee-to-role mapping lives in app-managed directory data"
    - "The same assignee cannot drift between multiple role/rate identities once normalized"
    - "Average-rate fallback is a first-class policy and excludes invalid or empty rates"
  artifacts:
    - path: "src/lib/costing/types.ts"
      provides: "Canonical cost engine contracts for directory entries, warnings, and totals"
      min_lines: 60
      contains: "AssigneeDirectoryEntry"
    - path: "src/lib/costing/assigneeDirectory.ts"
      provides: "Normalization, lookup, and average-rate fallback helpers"
      min_lines: 100
      contains: "resolveAssigneeDirectoryMatch"
    - path: "src/lib/costing/assigneeDirectory.test.ts"
      provides: "Regression coverage for normalized lookup and fallback averaging"
      min_lines: 100
      contains: "average rate"
  key_links:
    - from: "src/lib/costing/assigneeDirectory.ts"
      to: "src/lib/ingestion/types.ts"
      via: "directory lookup consumes snapshot issue assignee identity"
      pattern: "SnapshotIssueNode"
    - from: "src/lib/costing/assigneeDirectory.ts"
      to: "src/lib/costing/types.ts"
      via: "lookup returns typed role/rate resolution"
      pattern: "AssigneeDirectoryEntry"
---

<objective>
Create the phase-three assignee directory contract.

Purpose: Lock the source of truth for roles and rates before building money aggregation or UI.
Output: Costing types, assignee normalization/lookup helpers, and tests for directory resolution plus average-rate fallback.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-cost-engine/03-CONTEXT.md
@.planning/phases/03-cost-engine/03-RESEARCH.md
@.planning/phases/03-cost-engine/03-VALIDATION.md
@src/lib/ingestion/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the costing contracts for assignee mapping and warnings</name>
  <files>src/lib/ingestion/types.ts, src/lib/costing/types.ts</files>
  <read_first>src/lib/ingestion/types.ts, .planning/phases/03-cost-engine/03-CONTEXT.md, .planning/phases/03-cost-engine/03-RESEARCH.md</read_first>
  <action>Extend the snapshot issue contract only as far as needed to carry assignee identity into the cost engine. Create `src/lib/costing/types.ts` with the canonical Phase 3 models: allowed direction keys, assignee directory entry, directory resolution result, cost warning codes, ledger row, per-direction totals, and requirement profitability result. Keep the contracts explicit about `unmapped` and missing-assignee cases so later UI work does not invent its own semantics.</action>
  <verify>grep -q "AssigneeDirectoryEntry" src/lib/costing/types.ts && grep -q "unmapped" src/lib/costing/types.ts && grep -q "assignee" src/lib/ingestion/types.ts && npm test</verify>
  <acceptance_criteria>
    - `src/lib/costing/types.ts` contains `AssigneeDirectoryEntry`
    - `src/lib/costing/types.ts` contains `DirectionKey`
    - `src/lib/costing/types.ts` contains `unmapped`
    - `src/lib/ingestion/types.ts` contains assignee identity for cost calculations
  </acceptance_criteria>
  <done>Phase 3 has a typed contract for rates, roles, warnings, and output totals.</done>
</task>

<task type="auto">
  <name>Task 2: Implement assignee normalization, lookup, and average-rate fallback with tests</name>
  <files>src/lib/costing/assigneeDirectory.ts, src/lib/costing/assigneeDirectory.test.ts</files>
  <read_first>src/lib/costing/types.ts, src/lib/ingestion/types.ts, .planning/phases/03-cost-engine/03-CONTEXT.md, .planning/phases/03-cost-engine/03-VALIDATION.md</read_first>
  <action>Create `src/lib/costing/assigneeDirectory.ts` with helpers to normalize assignee identity, resolve a snapshot issue against the directory, and compute the valid average fallback rate. Preserve separate outcomes for `MISSING_ASSIGNEE` and `UNMAPPED_ASSIGNEE`, even though both later route cost into `unmapped`. Add `src/lib/costing/assigneeDirectory.test.ts` covering normalized matches, duplicate-resistant lookup, valid-average calculation, and empty/invalid-rate rejection.</action>
  <verify>grep -q "resolveAssigneeDirectoryMatch" src/lib/costing/assigneeDirectory.ts && grep -q "average rate" src/lib/costing/assigneeDirectory.test.ts && npm test</verify>
  <acceptance_criteria>
    - `src/lib/costing/assigneeDirectory.ts` contains `resolveAssigneeDirectoryMatch`
    - `src/lib/costing/assigneeDirectory.ts` contains average-rate fallback logic
    - `src/lib/costing/assigneeDirectory.test.ts` contains `average rate`
    - `src/lib/costing/assigneeDirectory.test.ts` covers missing and unmapped assignee outcomes
  </acceptance_criteria>
  <done>The codebase has a stable and tested source of truth for assignee role/rate resolution.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `grep -q "AssigneeDirectoryEntry" src/lib/costing/types.ts`
- [ ] `grep -q "resolveAssigneeDirectoryMatch" src/lib/costing/assigneeDirectory.ts`
- [ ] `grep -q "average rate" src/lib/costing/assigneeDirectory.test.ts`
- [ ] `npm test`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The role/rate source of truth is centralized and testable
- Fallback average-rate policy is encoded once and ready for later money aggregation
  </success_criteria>

<output>
After completion, create `.planning/phases/03-cost-engine/03-01-SUMMARY.md`
</output>

